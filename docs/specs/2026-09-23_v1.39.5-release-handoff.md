# v1.39.5 发布交接

日期：2026-09-23  
状态：已提交、已推送、已部署；等待下一轮管理员登录后的页面验收。  
部署目标：`main` / `463cab383aaaba679bbce3de01615640740380d6` / `v1.39.5`

## 先看结论

本次发布包含证据门修复、最终回复评估修复、设置页语义检索状态文案合并，以及发布脚本在 macOS Bash 3.2 下的变量展开修复。API-only 部署已完成，PostgreSQL 未重启，数据库迁移版本没有变化。

生产 `/health` 仍返回 `{"status":"degraded"}`。该状态在部署前已存在，本次部署前后未出现新的容器重启或日志异常；组件级原因本轮没有通过管理员诊断页复核。

## 变更与来源

| 内容 | 代码位置或提交 | 结果 |
| --- | --- | --- |
| 未知借用请求、同主题费用冲突、主题属性归属 | `src/homestay_bot/services/knowledge_evidence_policy.py` | 未确认请求不再承诺未知物品；冲突先于选首条答案检查；另一主题的时间不再给当前主题背书 |
| 静态服务费与实时房价边界 | `src/homestay_bot/services/answer_policy.py`、`src/homestay_bot/integrations/deepseek_client.py` | 停车等审核服务费可回答；房价、房态、订单、退款仍需实时边界 |
| 延迟退房属性校验 | `knowledge_evidence_policy.py::_SPECIAL_QUALIFIERS` | 普通退房时间不能证明可延迟退房；收费、节假日限制和否定规则保留原文 |
| 最终回复评估 | `tests/unit/test_knowledge_retrieval_eval.py::reply_passes_oracle` | 不再以 grounded 或字符串变化替代最终事实校验 |
| 设置页语义检索状态 | 合并提交 `e7ef7a2` | 区分 key 已保存与功能已开启 |
| 发布脚本变量边界 | `run-deploy.command`，提交 `463cab3` | Bash 3.2 下成功和错误输出均使用明确变量边界 |

## 验证证据

### 源码与测试

- 证据门相关测试：45 项通过。
- 版本检查：5 项通过。
- 证据门修复后的全量离线基线：1794 passed / 24 skipped / 0 failed；语义检索、外部契约和 PostgreSQL 跳过项不计作通过。
- `ruff`、`mypy`、`git diff --check` 已通过。
- 发布前 `./run-deploy.command --check` 通过。
- 未跟踪的 `docs/knowledge/` 按项目规则保留，没有进入发布包；未读取或提交受保护的项目总结文件。

### Git 与发布包

- GitHub `origin/main` 已指向 `463cab383aaaba679bbce3de01615640740380d6`。
- `v1.39.5` 已推送，发布脚本生成并验证 bundle 后才上传服务器。
- 最终工作区的 tracked 文件干净；`docs/knowledge/` 是既有未跟踪内容。

### 服务器备份与部署

部署前备份目录：`/opt/yumi-backups/pre-v1.39.5-20260922T210937Z`。

- PostgreSQL custom dump：37 张表、366 个归档对象、355884 字节；`pg_restore --list` 可读性复验通过。
- 源码归档和私有上传目录均已保存并可读取；备份文件权限按部署脚本设置为仅所有者可读写。
- 服务器部署前 HEAD：`73a898305676180bc61050aac47cdda99f17adee`。
- 服务器部署后 HEAD：`463cab383aaaba679bbce3de01615640740380d6`。
- 仅重建 API 容器；PostgreSQL 容器启动时间保持 `2026-08-10T16:43:53.783849747Z`，重启次数为 0。

### 运行态与公网

- API 容器包版本：`1.39.5`。
- API 状态：`running`，`restarts=0`，以 `uid=10001(app)` 运行。
- Alembic：`0027_knowledge_embeddings (head)`。
- 本机和公网 `openapi.json` 版本均为 `1.39.5`。
- 本机和公网 `/health` 均应答 `{"status":"degraded"}`；该状态是部署前基线，不能当作本次发布引入的故障，也不能当作全部健康。
- 公网 `/employee/login`：200。
- 未登录 `/employee/approvals`：401，认证边界正常。
- 登录页安全响应头：`no-store`、`frame-ancestors 'none'`、`DENY`、`no-referrer`、`nosniff`。
- 部署后 API 日志异常扫描：0；启动日志正常。
- 私有上传挂载：`/opt/yumi-data/private_uploads -> /app/data/private_uploads`，当前文件数为 1。
- 部署脚本远端门禁输出：`DEPLOY_OK`；本地收尾输出：`部署完成（v1.39.5）`。

## Evidence → Finding → Path

| Evidence | Finding | Path |
| --- | --- | --- |
| 服务器 HEAD、容器包版本和公网 OpenAPI 均为 1.39.5 | 源码、部署副本和运行版本一致 | 发布脚本、服务器 `/opt/yumi-homestay-bot`、`/openapi.json` |
| 37 张表、366 个对象的 dump 可由 `pg_restore --list` 解析 | 部署前数据库备份可恢复性证据存在 | `/opt/yumi-backups/pre-v1.39.5-20260922T210937Z` |
| PostgreSQL 启动时间和重启次数未变 | API-only 重建没有重启数据库 | `docker inspect yumi-homestay-bot-postgres-1` |
| `/health` 前后均为 degraded，日志异常扫描为 0 | 既有降级状态未显示为本次回归 | `/health`、API 容器日志；组件原因仍待管理员诊断页确认 |
| `/employee/login` 为 200，未登录审批为 401 | 公网登录入口和未认证边界可达 | `https://akros.icu/employee/login`、`/employee/approvals` |

## 尚未完成的验收

1. 当前 Chrome 管理后台登录态已失效，未完成登录后的诊断页和业务页面人工验收；需要管理员在 Chrome 登录后再做只读检查。
2. 未发送企业微信测试消息，因此没有客人实际收件证据；本次仅做部署运行态和 HTTP 边界验收。
3. `/health` 的组件级 degraded 原因未在管理员诊断页复核。历史记录指向百居易回调未接通，但本报告不把历史记录当作本次确认。
4. 语义检索仍默认关闭，生产知识库录入数量和线上语义召回未在本次部署中改动或重新验收。
5. PostgreSQL 并发锁场景没有在本次本机测试中执行；部署脚本只证明生产 dump 可读和迁移版本一致。

## 回滚入口

优先只回滚代码：使用备份目录中的 `git-head.txt` 恢复服务器源码，再只重建 API 容器。只有确认数据迁移或数据写入需要回滚时，才按同目录 `RESTORE.md` 停 API、恢复 `homestay.dump`，并用 `row-baseline.txt` 复核表行数。

本版本没有数据库迁移，因此默认回滚路径是代码回滚，不恢复数据库。

## 下一位执行者

先让管理员在 Chrome 登录后台，进入诊断页只读确认 degraded 组件；然后在不改生产数据的前提下核对版本、知识库启用数量和语义检索开关。若要做企业微信真实消息验收，另行记录入站消息 ID、最终消息 ID、任务 ID 和客人实际收件结果，不能用 `/health` 或 API accepted 替代。
