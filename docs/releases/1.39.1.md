# 1.39.1 发布变更记录

> 2026-09-24 补写，来源见 [README](README.md#补写说明2026-09-24)。

## 版本与范围

| 项 | 内容 |
| --- | --- |
| 版本 / 日期 | 1.39.1 / 2026-09-23 |
| 提交 / 标签 | `e7ef7a2`，标签 `v1.39.1` |
| 部署目标 | 未单独部署。1.39.3 合并本改动，随 1.39.5 上线 |
| 数据库迁移 | 无 |

同一时段 Codex 在另一分支也做了一个标为 1.39.1 的提交（`f05dd57`），后来以 1.39.2 发布。两条分支互不包含对方，由 1.39.3 合并。

## 改动清单

| 改在哪里 | 改前 | 改后 |
| --- | --- | --- |
| `templates/admin/settings.html` 语义检索分组 | key 旁显示「已配置」，容易理解成功能已生效；不勾选开关保存时，key 不测试也不启用 | 顶部状态说明分三种：未填写、已保存未开启、已开启（通过连接测试）；「已配置」改为「已保存」；开关文字注明「保存时测试 key，通过才生效」 |
| `tests/integration/test_runtime_config_routes.py` | — | 新增三种状态的页面断言 |

不改任何保存或测试逻辑。

## 改动凭据

- **用户原话**：「为什么填了key还是显示未配置」，接着是「现在显示已配置，但是为什么语义检索还能继续勾选，非常不清晰」。
- **用户确认**：方案说明后回复「可以」。

## 验证证据

| 类型 | 结果 |
| --- | --- |
| 全量离线测试 | 1785 passed / 24 skipped |
| CI | 分支 35783800739、main 35784207924、标签 35784212086，均 success |
| 部署与运行态 | 本版本未单独部署，见 [1.39.5](1.39.5.md) |
| 登录后的页面人工验收 | 未记录。1.39.5 交接记载当时管理后台登录态已失效 |

## 风险与未覆盖

只改文案与状态展示，风险低。页面实际显示效果没有登录后的截图或人工核对证据。

## 回滚

随 1.39.5 一起回滚，见 [1.39.5](1.39.5.md)。
