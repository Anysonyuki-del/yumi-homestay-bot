# 1.39.14 发布变更记录

## 版本与范围

| 项 | 内容 |
| --- | --- |
| 版本 / 日期 | 1.39.14 / 2026-09-24 |
| 提交 / 标签 | 以标签 `v1.39.14` 指向为准，发布后在本文追加 |
| 部署目标 | 生产，仅重建 API 容器；不重启 PostgreSQL |
| 数据库迁移 | 无 |

## 改动清单

| 改在哪里 | 改前 | 改后 |
| --- | --- | --- |
| `services/conversation_service.py::_stage_fast_ack`、`_LIVE_SEARCH_ACK` | 只有服务请求会先发安抚，联网查询要等到最终答案 | 联网查询先发固定话术，不调用模型，不加天气开场白，也不加转人工收尾；服务请求与设施故障的处理不变 |
| `integrations/deepseek_tourism.py::DeepSeekTourismSearcher.search` | `max_uses=2`；未设置思考；推荐类正文 700 至 900 字 | `max_uses=1`；`thinking: disabled`；推荐类正文 400 至 600 字 |

## 改动凭据

| 改动 | 依据 |
| --- | --- |
| 全部 | 1.39.13 测试号验收时，用户反馈「用时有点长」。生产数据库时间线显示两条联网问题分别等了 30 秒和 21 秒，其中联网搜索 17 秒和 12.5 秒，期间没有任何回复。用户回复「按顺序全部做完」。Spec 为 `docs/specs/2026-09-24_live-reply-latency-spec.md` |
| 搜索参数 | 按 Spec §3 实测前定下的标准取舍，数据见 Spec §6；跳过精炼（R1）未达标准，没有采用 |

## 验证证据

| 类型 | 结果 |
| --- | --- |
| 回归测试 | 新增 4 项安抚测试，其中联网安抚一项修改前失败、修改后通过，其余 3 项用来防止误伤；搜索参数断言修改前失败、修改后通过 |
| 全量离线测试 | 1933 passed / 24 skipped |
| 静态检查 | Ruff、mypy 137 个源文件、`git diff --check` 通过 |
| 真实 DeepSeek（生产容器临时目录，工具用假数据，不写库、不发消息，结束后清理） | 基线与 5 个方案各 8 次，组合方案 8 次，全部满足质量底线。联网搜索中位数：基线 14.6 秒，组合方案 3.4 秒；端到端：17.3 秒降到 5.1 秒 |
| CI、部署前备份、部署后运行态 | 发布后在本文追加 |
| 真实外部验收（测试号收件） | 发布后在本文追加 |

## 风险与未覆盖

- 搜索次数减为 1，需要多方来源交叉核对的复杂问题可能查不全；关闭思考后，综合能力可能下降。实测 16 次都满足质量底线，但样本量有限。
- 缓存命中时，安抚和答案几乎同时到达。
- 企业微信送达（2.5 至 6.4 秒）和 3 秒合并等待不变，客人看到第一句回复仍需约 5 至 10 秒。

## 回滚

只回滚代码到 `v1.39.13`，只重建 API 容器；无迁移，不恢复数据库。
