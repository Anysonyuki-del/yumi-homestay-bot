# 1.39.4 发布变更记录

> 2026-09-24 补写，来源见 [README](README.md#补写说明2026-09-24)。本版由 Codex 实施。

## 版本与范围

| 项 | 内容 |
| --- | --- |
| 版本 / 日期 | 1.39.4 / 2026-09-23 |
| 提交 / 标签 | `73a8983`，标签 `v1.39.4` |
| 部署目标 | 未单独部署，随 1.39.5 上线 |
| 数据库迁移 | 无；不影响应用代码 |

## 改动清单

| 改在哪里 | 改前 | 改后 |
| --- | --- | --- |
| `run-deploy.command`（发布脚本） | macOS 自带 Bash 3.2 下，输出密钥路径的变量展开有边界问题，自动部署中断 | 变量加明确边界，恢复自动部署 |

本提交同时把发布脚本加入跟踪。脚本含服务器地址，已在 1.39.6 前停止跟踪（`f7137a9`），见 [1.39.6](1.39.6.md)。

## 改动凭据

依据是 CHANGELOG 的描述：自动部署在 Bash 3.2 下中断。**原始报错输出未入库**，本记录无法复核具体症状。

## 验证证据

| 类型 | 结果 |
| --- | --- |
| CI | main 35784700707、标签 35784700956，均 success。CI 不运行发布脚本，这只证明应用代码未受影响 |
| 脚本实际效果 | 由 1.39.5 的成功部署间接证明 |

## 风险与未覆盖

脚本改动没有自动化测试。公开仓库的历史里仍留有这版脚本（历史未改写）。

## 回滚

脚本已不在仓库中跟踪；应用代码无变化，无需回滚。
