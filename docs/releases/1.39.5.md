# 1.39.5 发布变更记录

> 2026-09-24 补写，来源见 [README](README.md#补写说明2026-09-24)。本版由 Codex 实施和部署，详细核对见 `docs/specs/2026-09-23_v1.39.5-release-handoff.md`。

## 版本与范围

| 项 | 内容 |
| --- | --- |
| 版本 / 日期 | 1.39.5 / 2026-09-23 |
| 提交 / 标签 | `463cab3`，标签 `v1.39.5` |
| 部署目标 | 生产，仅重建 API 容器 |
| 数据库迁移 | 无 |

本次部署首次把 1.39.1～1.39.4 的改动带上生产。

## 改动清单

| 改在哪里 | 改前 | 改后 |
| --- | --- | --- |
| `run-deploy.command` 错误路径与成功收尾 | Bash 3.2 下仍有两处变量边界问题 | 输出一律使用明确变量边界 |

随本次部署一并上线的应用改动见 [1.39.1](1.39.1.md)、[1.39.2](1.39.2.md)、[1.39.3](1.39.3.md)。

## 改动凭据

依据是 1.39.4 修复后发布脚本在其他输出路径上的同类问题，见 CHANGELOG 与交接文档。原始报错未入库。

## 验证证据

| 类型 | 结果 |
| --- | --- |
| 全量离线测试 | 1794 passed / 24 skipped，为证据门修复后的基线 |
| 其他测试 | 证据门相关 45 项、版本检查 5 项通过 |
| 静态检查 | Ruff、mypy、`git diff --check` 通过；发布前 `--check` 通过 |
| CI | main 35784847655、标签 35784848070，均 success |
| 部署前备份 | `pre-v1.39.5-20260922T210937Z`：37 张表、366 个对象、355884 字节，可读性复验通过；源码归档与私有上传目录均已保存 |
| 部署后运行态 | 服务器源码 `73a8983` → `463cab3`；容器包版本与公网 `openapi.json` 均为 1.39.5；API `restarts=0`，非 root 运行；PostgreSQL 未重启；迁移 `0027_knowledge_embeddings (head)`；日志异常 0；远端门禁 `DEPLOY_OK` |
| 公网边界 | 登录页 200，未登录审批页 401，安全响应头齐全 |
| 真实外部验收 | 未执行：没有发企业微信测试消息；管理后台登录态已失效，未做登录后的页面验收 |

## 风险与未覆盖

- `/health` 部署前后都是 degraded，组件原因未经诊断页核对。
- PostgreSQL 并发锁场景未在本机执行。

## 回滚

只回滚代码：用部署前备份中记录的源码提交恢复服务器源码，只重建 API 容器。本版本无迁移，不恢复数据库。
