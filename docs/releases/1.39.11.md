# 1.39.11 发布变更记录

## 版本与范围

- 日期：2026-09-24；目标标签：`v1.39.11`，发布提交以该标签指向为准。
- 部署目标：生产 API；不重启 PostgreSQL，无数据库迁移。
- 用户授权：「直接修」「继续中断的任务」「提交推送部署」。

## 改动清单与依据

| 文件与符号 | 改前 → 改后 | 凭据 |
| --- | --- | --- |
| `src/homestay_bot/application.py::reply_chain_undelivered_payload`、`_notify_undelivered_reply_chain` | 员工通知仅有失败段号 → 同时显示已有内部会话编号，不携带客人正文或外部身份 | 通知载荷缺编号及通知正文无法定位的失败测试；分段 Spec §6 |
| `application.py::_guest_reply_is_stale`、`_latest_conversation_handoff_id`、`TransactionalOutboxWeCom._enqueue_guest_text`、`_enqueue_guest_reply_continuation` | 续发只看员工消息 → 复用接管审计边界识别新接管，旧人工状态不拦截 | 真实入站「转人工」及共享接管入口回归；分段 Spec §6 |
| `services/guest_reply_policy.py::_opening_paragraph` | 独立标题遮住正文开场白 → 跳过开头独占一行的标题，检查首个正文段 | 中英文重复开场白先红后绿测试 |

Spec：`docs/specs/2026-09-24_split-long-replies-spec.md` §6。仅以上三项修复；版本号和更新日志为发布所需。

## 验证证据

- 离线全量：1875 passed / 24 skipped / 9 warnings，61.33 秒；相关回归 42 passed。
- Ruff（修改的源码和测试）、mypy（2 个修改源码文件）、diff-check 通过。
- 警告涉及依赖弃用、SQLite 连接清理与事件循环关闭；24 项跳过为真实外部契约及未配置的 PostgreSQL 用例。
- CI、构建、备份复验及部署后运行态：发布准备时尚未执行，完成后追加现场记录。
- 部署前 API 为 1.39.10，服务器源码为 `803637139`，API/PostgreSQL 均运行且重启次数 0；健康为 degraded，不能据此宣称全部组件健康。
- 真实外部验收：本次未执行；本次授权没有要求发送客人或员工测试消息。

## 风险、未覆盖与回滚

- 出站前检查到网络发送之间仍存在竞态；旧版本排队载荷无审计边界时沿用旧判定。
- PostgreSQL 并发和真实企业微信分段实收尚未验收；CI 的 PostgreSQL 步骤仅覆盖既有清理仓储测试。
- 无表结构变化，必要时恢复备份中旧源码/镜像，仅替换 API；不因代码回滚恢复业务数据库。
- 失败通知旧任务缺少会话编号时明确标记未知，不猜测客人身份。


## 2026-09-24 发布现场追加

- 发布提交：`92cfc656a24a8f4c3cce1f010b1546b094e020c7`；标签 `v1.39.11`，已推送 main 和标签。
- 本地 wheel 构建成功；CI `35952122540`、`35952122305` 均 success。CI 离线 1875 passed / 24 skipped / 17 warnings；隔离 PostgreSQL 回归 18 passed；镜像构建成功。该 PostgreSQL 回归不等同于分段接管并发验收。
- 配置备份标识 `pre-v1.39.11-config-20260924T033633Z`：旧源码、环境文件、Compose 原配置及解析配置、私有上传归档；归档目录可解析。
- 数据库备份标识 `pre-v1.39.11-20260924T034017Z`：37 张表、366 个归档对象、384441 字节；再次从落盘 dump 经标准输入交给 `pg_restore --list`，仍解析 366 项。备份目录 700、敏感文件 600。
- **首次部署失败与恢复**：部署执行脚本增加备份 `umask 077` 后，未在 Git 更新前恢复，导致新源码权限过严；API 的非 root 进程读 `pyproject.toml` 报 PermissionError 并重启。原部署脚本以失败退出，不能引用为 DEPLOY_OK。
- 按 Git 记录仅恢复受跟踪源码的 644/755 权限，并在备份阶段后恢复 `umask 022`；重新构建、替换 API 后独立逐项验收。没有改环境文件、上传文件或数据库内容；部署执行脚本为本地忽略文件，不属于发布源码。
- 恢复后 API 为 running、重启 0，包版本和公网 OpenAPI 均 1.39.11；服务器源码为发布提交，受跟踪工作区干净。
- PostgreSQL 启动时间与发布前一致，重启 0；Alembic current/head 均 `0027_knowledge_embeddings`。
- 内网与公网 `/health` 均 HTTP 503、degraded，与部署前基线相同；不是全部组件健康证明。登录页 200、未登录审批页 401；新 API 日志异常计数 0。
- 未执行登录后页面交互及真实客人/员工消息收件验收：本版无 UI 变更，本次未授权发送真实测试消息。此前版本的收件确认不替代本版本验收。
- 本追加记录随独立文档提交推送；运行代码及版本标签仍以 `92cfc65` 为准，不为记录更新重复重建 API。
