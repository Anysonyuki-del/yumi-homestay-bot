# 更新日志

本文件记录 YuMi 民宿 AI 的正式发布内容。版本号遵循语义化版本规则。

## [1.33.0] - 2026-09-11

客户档案增加两处手动维护入口。

**服务任务批量操作**：该栏此前完全只读，运营要清理某位客户的历史任务只能去任务
列表页逐条筛。现在可勾选批量归档与取消，直接提交到 `/employee/tasks` 的既有路由，
连同其 CSRF、权限与手输确认一并复用——本页不新增任何后端逻辑。

刻意不提供「永久删除」：它不可恢复且会写入 `purged_task_marks` 墓碑，应在任务列表
页配合完整筛选进行。可归档／可取消的判定放在服务层并复用 `ARCHIVABLE_TASK_STATUSES`，
与批量路由的校验是同一条规则；两处各写一份时，界面会出现「勾得上但提交被拒」的按钮。

批量令牌的实体号从 `tasks.py` 的私有常量提升为公开的 `BULK_TASK_CSRF_ENTITY`：两处
各写一个魔数 0 时，一旦有人改动就会让另一处的表单静默失效——CSRF 错配从界面上看
只是「点了没反应」。

**手动重算摘要与记忆**：此前只能等每小时的后台维护。新增按钮触发
`customer_context_refresh` 作业，与后台循环调用同一个 `maintain_customer`，不另立
一套生成逻辑；生成出的记忆仍是候选，仍需逐条人工批准。

采用入队而非同步：同步会把请求挂到模型超时上限（45 秒），也可能与后台维护撞上同一
客户的事务，因此页面文案只说「已排队」，不假装已完成。按客户设 10 分钟冷却窗口，
超出时明确拒绝而不是静默忽略——这个按钮每点一次都真实产生模型费用，而静默会让人
以为没生效而继续点。

顺带删除模板里的 `is_admin` 判断：客户详情页只有管理员到得了（非管理员 403），该
变量永远为真，留着会让人误以为这里还有一道权限分支。权限由路由层的 `_current_admin`
一处保证。

验证：全量 1591 项测试通过。四条守护中三条已确认能抓住对应回归；第四条原本断言
「非管理员看不到按钮」，红测时发现它实际验证的是整页 403、`is_admin` 判据从未被覆盖，
遂改为如实断言 403 并移除该死代码。

## [1.32.0] - 2026-09-10

紧急安全提示按危险类别分化，并修复两处让指令悄悄丢失的既有缺陷。

生产验收发送「房间里有煤气味」后，客人收到的是「请先确保自身安全，不要自行处理
故障。」。分类器已判成 gas 并写进审计（`emergency:gas`），文案却没用上这个结果——
`safety_reply` 只有 fire 有专属文案，gas、electric、medical、violence 全部退回同一句
通用文本。对燃气泄漏而言它信息量不足，客人可能留在房间里等管家，而开关一次电灯就
可能引爆；medical 不提 120、violence 不提 110 也是同类缺口。

`guest_reply_policy` 的高危安全句白名单里早已预留「开窗通风」「切断电源」
「拨打119/110/120」「呼叫急救」等词，说明按类别给指令本就是既定设计，只是实现停在
了 fire 一类。现补齐中英文各五类；access（进不去、被锁门外）不涉及人身危险，继续
使用通用文案。

两处既有缺陷由新测试挖出，均不报错、只让客人少收到内容：

- 英文火警文案的「Call 119 if there is fire or smoke.」不匹配白名单里按机构名列举的
  规则，一直被整句丢掉——英文客人遇火灾从未收到过报警号码。白名单补入数字报警号。
- 高危回复用空串连接各安全句，中文正常，英文会拼成「immediately.Call 119」。改用
  语言分隔符。

验证：全量 1584 项测试通过。首版测试只断言「剩余内容超过 10 字符」，判据过弱，未能
发现英文 gas 第二句被白名单滤掉；改为逐片段断言后才暴露，上述两处既有缺陷也是由此
挖出。三条守护均已确认能抓住对应回归。

## [1.31.0] - 2026-09-10

无人接手时删除第一人称软承诺与未经记录的应允。

生产消息 127 回了「明天下午三点左右到没有问题的。我这边先帮您确认一下安排。」，
129 回了「我好提前帮您留意。……我会替您留意的。」——两轮本轮任务 0、审批 0、
提醒 0、无管家通知：客人在等，系统里没人知道要做什么。同一形态两次复现。

现有 `_UNSAFE_COMMITMENT_PATTERNS` 要求「确定性副词 + 动作动词」（马上安排、会尽快
送、一定给您解决），上述两类整类漏过。新判据只在 `requires_human=False` 分支生效：
那一支意味着本轮不会产生任务、审批、提醒或管家通知；`requires_human=True` 分支会
追加「我会立即联系管家来处理」，同样的话有人接手，不该删。

刻意不追「如果您早到，我看看当天能不能先寄存」这类预告未来可能的说法：它们留了
余地，危害有限，而动词是开放集合，白名单穷举只会变成打地鼠。

已知代价：软承诺常附着在有用信息的后半句（「告诉我房间号，我好提前帮您留意」），
按整句删会把前半句的引导一并删掉，消息 129 因此从 110 字缩到 45 字。核对追问与
事实信息仍完整，暂接受此代价；按分句删除是可选改进，需单独验证读感后再定。

自查抓到一处误伤并修正：「具体门锁和寄存安排以我当天给您的确认为准」含「我给您…
确认」而被判为承诺，但整句作用恰好相反，是在降低期待，删掉比留着更糟。

验证：全量 1580 项测试通过。另用六条真实生产回复对照：120/123 零删除；113/115/125
各删其软承诺句，而入住 14:00、退房 12:00、证件、禁烟、22:00 等事实信息全部保留；
127、129 两条复现消息的目标句均被正确删除。

## [1.30.0] - 2026-09-10

删除模型写在正文里的来源说明，来源声明只由系统生成。

生产消息 125 的正文末尾出现「（以上天气信息来自武汉市气象台及中央气象台2026年
9月10日发布内容，仅供出行参考。）」，紧接着又是系统页脚的「这是我今天帮您查到的
最新预报」——同一件事说两遍。

更要紧的是它绕过了措辞控制：v1.28.0 把来源列举从页脚拿掉，正是为了避开企业微信的
安全限制；模型在正文里自由发挥的来源声明不受这个控制。这次措辞恰好没触发，下次
不一定，而且真触发了改页脚也救不回来。

正文清洗里本就有清除「参考来源：X」「查询日期：X」这类标签式写法的规则，漏的是
自然语句形态——与 v1.29.0 的设施断言同属一类：机制在链路上，判据形态不全。新判据
必须以「以上／上述／本回复」这类回指词起手，避免把「东湖的水来自长江」这种正常
句子当成来源说明删掉。

验证：全量 1575 项测试通过。另用本轮五条真实生产回复走完整清洗链路对照：113/115
零删除，123 只删设施断言，125 只删这句来源说明 44 字，天气事实、穿衣建议与系统页脚
全部完好。两条守护均已确认能抓住对应回归。

## [1.29.0] - 2026-09-10

删除模型编造的本店设施断言。

生产 2026-09-10 的天气回复写了「民宿这边：大堂备有薄外套和雨伞，需要随时说；房间
已换秋被，觉得凉可再加一床。」——知识库里没有这些事实，客人可能据此下楼索要并不
存在的东西。

未经审核民宿自述的过滤机制一直在链路上，漏的是判据形态：它只认「我们民宿」「本店」
这类自称词。上述前半句的说法不在词表里，后半句更是一个自称词都没有，两句都被放行。
用「有没有提到民宿」识别「有没有编造民宿事实」是不可靠的代理，因此新增一条按危害
本身取形的判据：对本店场所（大堂、前台、房间、楼下等）里某样东西的供应或状态下断言。

该函数只作用于联网／旅游正文与改写路径，而联网搜索不可能返回本店信息，因此这条
路径上出现的设施断言必然是模型自行添加的，从严删除是安全的。只覆盖中文：生产证据
全部是中文，英文侧未观察到同类写法，凭印象扩正则会在没有证据处引入误删风险。

验证：全量 1573 项测试通过。误删比漏删更难发现——回复会残缺却照样发得出去，没有
任何失败信号——因此除单元测试外，另用本轮四条真实生产回复做对照：113/115/120
一字未删，123 仅删去那一句 38 字，其余 334 字完整保留。

## [1.28.0] - 2026-09-10

联网回复不再列举来源，只保留时效声明：「这是我今天（9月10日）帮您查到的最新预报。
天气可能临时变化，出门前可以再看一眼实时情况。」

依据是六条真实样本的完美分离：带来源句的三条全部被企业微信以 `wecom_async_13`
（官方定义为「安全限制」）拦下，不带的三条全部送达。其中一对是同一条消息的严格
前缀——被拦的 120 去掉后半段就是通过的 121，长度、行数、换行、项目符号、年份、
摄氏度、天气词汇均已在此前样本中逐一排除。

坚持列举来源还有个自伤后果：被拦后走兜底重发，而兜底只保留前半段事实，会把
「天气可能临时变化」这句提醒一起丢掉——为了标注依据，客人反而连时效提示都收不到。

诚实性改由「我今天帮您查到的」加「可能临时变化」承担；仍然要求确有搜索来源，
没有任何结果时不得声称查过。随之删除只为拼来源名存在的机构名映射与标题判据。

修复一处本次改动引入的缺陷：页脚句读从「最新预报，主要参考了…」变成「最新预报。」，
而识别模式写死了逗号，导致收尾拆不出来、会被当作正文送进改写与精炼。模式改为
来源子句连同其前置逗号一并可选，同时兼容库中存量旧格式正文。

验证：全量 1570 项测试通过。新增陈旧来源过滤的行为级覆盖——此前该过滤器唯一的
断言依赖页脚点名来源，随本次改动会失去全部覆盖。四条守护均已确认能抓住对应回归。

## [1.27.0] - 2026-09-10

联网回复不再把搜索结果标题当作来源机构名念给客人。

生产天气回复拼出过「主要参考了武汉天气预报15天天气、最低气温17℃，今早出门加件外套
等公开信息」——两个都是原始网页标题。根因是 `_source_display_name` 的顺序反了：已知
机构名表只在标题为空时才查，正常有标题时直接返回网页标题。

改为域名优先：命中已知机构则按机构名念（域名是稳定事实，标题是搜索结果的一次性
产物）；未知域名时标题必须先通过「能不能当机构名念」的判据——出现句读或数字量值、
或超出长度上限的一律拒绝。长度上限按语种分开，中文按字数、拉丁文按词数，避免误杀
`Wuhan Meteorological Service` 这类正当来源名。拿不到可读来源时整条联网回答降级，
与既有「不拿域名或模型常识冒充客人侧依据」的取舍一致。

企业微信 `fail_type=13` 经官方文档确认为「安全限制」（接收消息和事件）。生产两次被拦
的回复都带上述来源句，是否由它触发仍待一次真实天气提问验证。

验证：全量 1570 项测试通过。新增 3 项直接使用生产上那两个真实标题作为用例，另更新
一条断言原始标题回显的旧测试；三条守护均已确认能抓住对应回归。

## [1.26.0] - 2026-09-10

投递失败可诊断性三项。真实微信号验收暴露：一次普通天气问答被企业微信以
`wecom_async_13` 拦下，系统自动改写重发并通知了管家，客人只收到一条兜底回复——
链路是通的，但事后完全查不出「为什么」。

改写兜底原因落库。改写器有二十多种拒绝理由（事实被改写、含链接、含联系方式……），
过去 `except` 把异常整个吞掉，数据库里只剩一个布尔值。现在记入
`delivery_rewrite_fallback_reason`。

被拦正文的结构快照。消息正文 7 天后进保留期被清空，于是这个系统最主要的故障模式
（生产 6 次失败全是 `wecom_async_13`）事后只剩错误码，5 次的正文已永久消失。现在
失败当时固化形态特征（长度、行数、有无换行/列点/Markdown/链接/长数字串），只记形态
不记正文，不给保留期开后门。

DeepSeek 调用上台账。`external_requests` 此前只有 hostex，最贵也最易出问题的外部
依赖完全不上账。改为在传输层记账，六个 DeepSeek 服务一次覆盖；超时与连接失败同样
记录，只挂响应回调会把这类整体漏掉。台账只取方法与路径，不含查询串。

验证：全量 1567 项测试通过。新增 11 项覆盖兜底原因、快照不泄漏正文、快照真的落库、
台账的成功/失败/异常/写入失败/查询串五条路径，均已确认能抓住对应回归。

## [1.25.0] - 2026-09-10

系统诊断新增「客人消息未送达与重试」看板。按投递链而不是消息行计数：一次失败与
它的改写重发在数据库里是两条消息，按行统计会把同一次未送达数成两次，也说不清
它了结没有。四个阶段——重试在途、已改写重发并受理、仍未送达但已通知管家、
仍未送达且无人知晓——只有最后一档用告警色并给出处理方法，其余是中性事实。

看板只读投递相关的机器码字段：消息编号、阶段、尝试次数、错误码与失败时间，
不含消息正文、客户身份或会话链接。脱敏诊断报告同步带上这四个数，避免报告看着
一切正常而卡住的客人消息只在页面上。读取失败时显示「暂时读不到」而不是 0。

生产当前：4 次未送达，其中 3 次改写重发已被受理、1 次已通知管家、0 次无人知晓。

验证：全量 1557 项测试通过。新增十条守护，含真实数据库上的 JSON 取值、上限截断
标记、链归并与阶段判定，以及页面配色与「只报数不说怎么办」两条表达约束。

## [1.24.0] - 2026-09-10

修复「重试在途」标记永远清不掉。`delivery_retry_pending` 压制失败通知，等的是
重试的结果，但代码只在终态失败补偿里清它；「重试被受理」与「重试失败且已就地
通知员工」这两条正常出口都没清，闩锁于是永久留在原消息上。两处出口各补一次收敛。

迁移 `0026_settle_retry_latch` 收敛存量：判据严格对应那两条出口，逐条要有证据
（重试已被受理，或重试失败但已登记员工通知），不按时间或数量一刀切。只改这一个
布尔标记，不动 delivery_status，也不补发通知——受理不等于送达，历史消息不该因为
一次数据收敛重新惊动员工。生产三条历史标记（消息 52、55、105）符合判据。

验证：全量 1544 项测试通过。新增闩锁生命周期三例（受理清、失败已通知清、确无
结果不清）与迁移两例，均已确认能抓住对应回归。

## [1.23.2] - 2026-09-10

日期段高度统一。此前每段高度等于该段恰好有几笔订单，7 天在手机上切成 3+3+1，
末段只有一笔就矮一截，整块日历参差不齐。现在每段固定预览三条：不足三笔留空行，
超过三笔仍用原生折叠展开（阈值从四条改为三条）。代价是订单少的段留有空行，
换来同一列表里各房间卡片的日历高度一致。

验证：全量 1539 项测试通过。新增守护用「各段笔数不同」的夹具断言各段轨道等高，
并已确认它能抓住按笔数缩水的旧行为。

## [1.23.1] - 2026-09-10

房间卡片配色收回设计令牌。v1.23.0 把 --primary、--muted 等换成了邻近字面色，
这一页因此长出第二套强调色，与控制台其它页面不一致。日历四档条色改为挂在
--primary 上（color-mix，附等价回退），换主色时整页跟着走。

同一笔订单被日期段边界切开时，两段都标注「同一笔，接上段／续下段」，悬停说明
与读屏文案共用同一句；此前两段各显示一行同名同晚数，与「一笔连住被拆成两单」
的错误数据长得一样。日期段不再横向滚动，去掉已无作用的键盘停留点，区域名保留。

验证：全量 1538 项测试通过。新增守护覆盖三类回归：字面色绕过令牌、color-mix
回退值与 mix 脱钩、条色改挂非主色令牌，另有跨段延续标注与焦点位的浏览器断言。

## [1.23.0] - 2026-09-10

房间卡片重排：紧凑客人摘要、突出倒计时，下一单按订单 ID 使用深靛蓝强调。
日期栏缩为 44px，取消横向滚动；桌面按最多 7 天、窄卡片按最多 3 天纵向分段。
住宿区间独立分轨，跨段保留 15:00 入住／12:00 退房的真实比例与延续标记。
手机补充完整姓名链接；每段超过 4 笔可用原生折叠展开，无 JavaScript 也可操作。
准备记录说明移入展开区，保留原有客户链接、冲突提示、CSRF 和未保存保护。

验证：全量 1534 项测试通过，含跨段几何、无脚本键盘展开、倒计时和表单回归；
桌面与 390px 手机合成数据页面确认没有横向溢出。无数据库迁移或新增依赖。
两条守护旧横向滚动机制的静态断言改绑新机制：日期段能否读清现在取决于
容器查询是否命中，因此改为守住 container-type 与宽／窄段长这条链路。

## [1.22.0] - 2026-09-09

重设计房间日期日历的日期栏与网格层次。住宿条配色保持不变。

### 日期栏成为真正的坐标带

此前日期栏高 52px、日期号仅 13px，在 190px 宽的列里被留白吞掉，比例撑不住。现在：
日期栏 52px → 64px，铺 `#F8FAFC` 与白色轨道分离，底部用 `#CBD5E1` 实线作为轴线锚定；
日期号 13px → 20px/600 深墨色，星期 11px → 12px，两者拉开明确的字号与字重台阶；
今日整列底色从表头贯通到轨道，日期号转主色并带小号「今天」标记。轨道上下留白
8px → 12px，与更高的日期栏成比例。

### 网格线不再融进背景

表头竖线此前用 `#F1F5F9`，在白底上对比度约 1.10:1，肉眼等同于没有线；轨道竖线却用
`#E2E8F0`——两套值不一致且前者不可见，网格结构读不出来。现在统一到日历局部变量
`--cal-rule: #E2E8F0`（轴线 `--cal-axis: #CBD5E1`），列从表头一路连到轨道底部；网格
底边由 `.cal__content` 自己闭合，不再依赖行程折叠是否渲染；重叠提示文案改为与模块
左对齐。日历仍不加外框（避免卡片套卡片），层次全部来自坐标带与统一线条。

### 防复发

新增资产断言：日历线条变量相对白底至少 1.2:1，且日历内不得再出现近白硬编码线条色。
该问题已出现两次，改为由测试钉住。
## [1.21.3] - 2026-09-09

修 v1.21.2 的排版问题：日历又被压到横向滚动、切掉末尾日期。保留 v1.21.2 的视觉设计
（配色、留白、日期线、事件分组、卡片圆角与内距均未改动），只调整版面宽度与分栏阈值。

### 根因

房间卡片并排阈值 `minmax(min(100%, 520px), 1fr)` 配合 `max-width: 1240px`，在任何桌面
宽度都只能给出约 582px 的卡片；而默认 3 天视图有 6 个日期列、每列最小 104px＝624px，
加卡片左右内距 44px 需要 668px。日历因此被压到最小列宽后仍溢出，只能横向滚动并切掉
末尾日期（实测 1280/1440/1600/1920 四档全部溢出，1024 单栏反而正常）。

### 修复（扩大版面，不改设计）

- 并排阈值 520px → 700px，列表上限 1240px → 1520px：宽屏仍并排且每栏约 740px 装得下
  日历，不够宽时自然回落单栏。
- 「入住安排」页单独放宽阅读宽度（`--measure-page` 1240px → 1560px），其余页面维持全站
  1240px 不变。
- 7 天及以上跨度固定单栏（10–17 个日期列并排必然滚动）。
- 实测 1024/1280/1440/1600/1728/1920/2560 七档，日历均无需横向滚动；≥1728 恢复并排。

### 防复发

新增资产断言：并排阈值必须 ≥668px（6×104＋卡片内距），低于此值直接失败。该问题已复发
两次，改为由测试钉住。
## [1.21.2] - 2026-09-09

- 房间卡片限制阅读宽度，宽屏按可用空间并排、手机单列，减少横向留白。
- 客人、计划时间和倒计时分层显示；日历去除重复外框和厚重底色，保留日期对齐、今日高亮和可点击住宿条。
- 普通下一步操作改为轻量工具行，逾期仍保持警示；手机日历继续局部横向滚动。
- 本次仅调整样式，无数据库迁移、依赖或业务状态变化。

## [1.21.1] - 2026-09-09

修 v1.21.0 现场反馈：桌面双栏卡片把日历挤到不可读。

- 房间卡片双栏（`@media(min-width:1024px)`）时每栏内容区仅约 450–560px，而默认
  3 天视图需要 6 个 104px 日期列（约 624px），日历因此在卡内横向滚动、切掉末日，
  「格子完全不可读」。改为**房间卡片始终单栏**：日历拿到整卡宽度，3/7 天视图在任何
  桌面宽度都能一屏看全所有日期列（单日列宽 = max(104px, 内容宽度/列数)）；14 天等
  长跨度仍在模块内横向滚动。移除不再需要的半宽 `--wide` 兜底。

## [1.21.0] - 2026-09-09

房间日期网格视觉优化：把「日期标题下堆圆胶囊」改成可对齐、可扫读的连续房间日历
（实施 `docs/specs/2026-09-09_room-calendar-visual-optimization-spec.md`）。只读展示，
无新增迁移/依赖/API/拖拽/审批。

### 几何与轨道（Spec §6）

- 住宿条按计划半天边界定位：计划开始＝入住日 15:00，计划结束＝退房日 12:00，裁切到
  窗口后按小时占比换算成相对内容宽度的百分比；日期头与住宿条共用同一坐标系，端点
  与日期网格对齐（104px 日宽下一晚约 91px、同日换客 12–15 点间隔约 13px）。
- 非重叠订单复用同一轨道（按计划起止 + 订单号稳定排序的扫描分配），不再按列表下标
  逐单换行形成楼梯；真实重叠才新增轨道并给「时间重叠，需核对」提示，行数反映最大
  同时重叠量。退房不晚于入住的异常区间不伪造几何，仅标记待核对。

### 视觉（Spec §5）

- 日历模块：白底细边框、双行日期头（星期＋日期，跨月首日显示月份）、今日整列浅蓝底、
  贯穿轨道的日期竖线。住宿条改为 6px 圆角矩形（不再满圆胶囊），按计划态配色：
  未来 / 覆盖当前时刻 / 已过去 / 已核验退房 / 重叠 各有语义色，对比度实测均 ≥4.5:1。
- 整条住宿即原生客户链接，命中高度 44px；无客户号显示「未关联客人」不生成链接。
  仅一个横向滚动容器同时滚动日期头与轨道；提供「查看本房间行程列表」折叠作为窄条与
  键盘/触屏的补充。

### 范围

- 保留既有订单事件倒计时、客户入口、认证、CSRF、未保存保护与订单事实边界。无已批准
  延迟时刻或客人 ETA 数据，不显示推测延迟。真实姓名不进入代码、测试与截图。
## [1.20.2] - 2026-09-09

修 v1.20.0 住宿条的两个问题（用户现场反馈）。

### 日期与住宿条对齐

- 时间轴此前拆成「日期」「住宿条」两个独立子网格，各自排布导致住宿条整体错位到
  右侧、与日期列对不上。改为**单一网格**：日期表头占第 1 行，每条住宿条占其后一
  行，列由同一 grid-template-columns 定义，日期与住宿条天然对齐。

### 同一客人的连续入住不再拆成两单

- 百居易会把同一客人的续住拆成多笔订单，甚至各自一个客户号（去重漏合），导致一位
  客人的连续住宿被显示成「今日到店 + 计划周转间隔」的换客周转，时间轴上也裂成两条
  1 晚。新增 `_merge_consecutive_stays`：相邻订单「前一笔退房日 == 后一笔入住日」且
  「同一客户号或同一非空姓名」合并为一段连续住宿，主状态、事件、住宿条、下一步与
  到离店计数全部基于合并结果。不同客人的背靠背订单仍是周转，不被误合并；仍保留首笔
  订单号作为客户链接，不改写底层订单数据。
## [1.20.1] - 2026-09-09

补 v1.20.0 遗漏的 A14：同步过期时事件行倒计时加「按上次同步计划，待核实」注记，
不把上次同步的计划时间冒充成实时（Spec §6.1 stale 行）。

## [1.20.0] - 2026-09-09

房间卡片给出跨日期住宿条、可点击客户姓名、按同一订单绑定的退房／入住倒计时与计划
周转间隔。实施 `docs/specs/2026-09-09_room-stay-timeline-countdown-spec.md`，只读展示，
不新增迁移、依赖、外部调用或订单/任务状态机改动。

### 住宿条时间轴与双事件

- `list_room_stays` 投影补订单 ID；`_room_events` 以订单为最小单位选出「本次退房」与
  「下一单入住」两个事件，姓名、日期、链接一律取自同一订单，绝不跨订单拼接
  （今日 A 退房 + B 到店 + 明日 C 到店时，退房=A、入住=B、C 只在时间轴出现）。
- 时间轴改为按日期网格的住宿条：每笔订单一条、独占一轨、按入住/退房日定位，超出
  当前窗口裁切并标前／后延续；姓名只出现一次并链接 `/employee/customers/{id}`
  （有 ID 无名显示「查看客人」，无 ID 显示「未关联客人」不生成链接）。
- 计划周转间隔仅在同日不同客人、且对应关系明确时显示（15:00 − 12:00，正常 3 小时）；
  连住（相邻订单同一客户）与非同日不显示。

### 倒计时区分计划与事实

- 退房用「计划退房」，到点只提示「退房待确认」，不断定超时占房；已核验退房
  （checkout_observed_on＝退房日）显示「已退房」且不倒计时、不伪造 12:00 实际时刻。
- 入住用「距可入住时间」「今天 15:00 起入住」，到点显示「到店待确认」，不宣称已到店。
- 绝对时间由服务端按 Asia/Shanghai 构造；前端单个页面级分钟定时器按「服务器观察
  时刻 + 页面经过时间」推进，不依赖设备时钟；跨计划节点／当地午夜／隐藏超一分钟恢复
  时，无未保存内容则单次 GET 刷新，有未保存内容只提示不导航（复用 dirtyForms）。
  无 JS 时保留绝对时间、状态词与客户链接。

### 卡片结构

- 移除标题旁孤立姓名、旧「下次入住／下次退房」区块与三段计划横幅；姓名归入有明确
  事件／区间的位置。近期安排时间轴默认展开，保留 3／7／14 天切换与横向滚动。

### 范围

- 无已批准延迟时刻或客人 ETA 的数据来源，本次不显示推断的延迟时间，不解析
  `LATE_CHECK_OUT` 任务正文；具备可信数据后另行扩展。真实姓名不进入提交文件或截图。
## [1.19.0] - 2026-09-09

房态与本地流程分清：入住安排做主信息，本系统准备记录降为辅助，缺记录显示「未确认」
而不是伪装成现实里的「未开始」。按用户明确的营业节奏（12:00 计划退房、12:00–15:00
周转清洁、15:00 起入住）与规则实现。

### 入住安排做主、本系统记录做辅

- 运营页标题改「入住安排」，主状态来自百居易订单事实；`RoomOperationalState` 记录
  降为辅助行，`NOT_STARTED` 显示「本系统准备情况：未确认」并注明「未确认不代表未
  入住或准备未完成」，中性色、不再触发红色告警。`OCCUPIED` 显示「本系统记录：已入住」，
  与订单占用分列来源。
- 入住状态标签按证据强度重写：`arriving_today` 今日到店、`occupied` 住宿期内、
  `departing_today` 今日离店、`vacant` 今日无订单占用（不再叫「近期空置」、不断言可售）、
  `unknown` 入住信息待核实。

### 按营业节奏与订单事实推导主状态

- 新增状态引擎 `AdminOperationsService._primary_status`，按订单事实 + 当地时刻推导：
  - **下一位到店即视为上一位已退房**：今日周转（不同客人）主行是到店客人，不再挂
    「退房待确认」。
  - **今日仅退房**：15:00 前显示「今日离店」，过 15:00 默认「按计划已退房」进入下一轮。
  - **同一客人连续订单为续住**，不显示成周转。
  - 只有 `checkout_observed_on` 等于退房日才算「已退房」（已核验），否则一律「按计划」。
  - 同步过期一律「入住信息待核实」，不推断空房或在住。
- 客人名显示在主状态与日期旁（`list_room_stays` 扩投影带 `customer_id`、`display_name`、
  `checkout_observed_on`；未关联客户时回落中性称呼，不编造）。
- 计划节奏条 `12:00 计划退房 → 12:00–15:00 计划清洁 → 15:00 起入住`，按当地时刻高亮
  当前时段；跨 12:00/15:00 后标签页重新获得焦点时只重新 GET 本只读页，不轮询、不提交。

### 页面结构与文案

- 全部房间一屏可扫读，需要关注的排前面；不再把在住房藏进「稳定房间」折叠。近期时间轴
  与「调整本系统记录」都收进各自展开区。房态下拉框改为「调整本系统记录」并注明「仅更新
  本系统记录，不办理百居易入住／退房」。
- 首页「当前房态」改名「本系统准备记录」，并说明这是本地准备流程分布、不是实际入住状态。
- `_next_step` 去掉「零开放任务=尚未生成相关任务」的假设，无待办显示「当前无待办」，
  零任务的到离店日引导到房间准备情况而非空任务列表。

### 范围

- 本轮为展示层，全部复用现有订单与本地记录，不新增数据库字段或状态枚举。延迟退房到
  具体时刻、真实到店事实写回入口等需要新增事实来源的部分不在本轮。
## [1.18.0] - 2026-09-09

管理员手动创建任务。此前任务只从百居易订单与生命周期派生，UI 无任何创建入口，
订单未覆盖的紧急事项（临时维修、加急补耗材等）无法录入。

### 新增

- 任务中心（管理员）新增「新建任务」表单与 `POST /employee/tasks/create`。
- 六类运营事项可手动创建：保洁、维修、补耗材、特殊服务、提前入住、延迟退房。
  `manual_contact` 由生命周期派生、涉及客户联系语义，排除在手动创建之外。
- 新任务默认进入待分派；在表单直接指定执行人则一步进入已分派。房间、服务日期、
  任务说明为必填——待分派及之后的状态受 `ck_business_task_execution_fields`
  约束要求房间与日期非空，校验在服务端插入前完成，被拒时返回具体文案而不是撞
  数据库约束。
- 新增任务来源 `BusinessTaskOrigin.MANUAL`，手动创建可追溯；不参与去重
  （`dedupe_key` 为空），不受周转墓碑影响。创建写审计
  `business_task_manual_created`。

### 约束

- 仅管理员可创建；`manual_contact` 不可手动建；不调用外部接口、不绕过状态机与
  数据库约束。建单 CSRF 用独立 entity，与批量、单任务令牌互不冲突。
## [1.17.0] - 2026-09-09

可用性审计报告 UX-01～11 全部实施（UX-07 已在 v1.16.0 完成）。四项范围决策由用户
一次性确认，记录在 `tasks/todo.md`。报告行号基于 ec7ca36，实施前已逐条对当时的
v1.16.0 代码重新取证，十条全部属实。

### 日常任务闭环（UX-01～04）

- **被拒时终于说得清缺什么。** `ReadinessRuleError` 原本继承 `ValueError`，在
  `page_errors.raise_page_error` 里落进通用分支，页面只剩追踪号和「任务操作未完成」。
  可它的消息（「至少需要一张有效现场照片」）本来就是写给执行员工看的。改继承
  `OperationRefused` 后原样送达；接口调用方仍得到明确的 JSON。
- 缺证据时不再渲染「确认房间可入住」——页面已经列出缺哪几项，再摆一个必然被
  拒的按钮，等于请员工先挨一次拒绝。改为指向补齐入口，服务端守卫不变。
- **详情页提交不再丢筛选与页码。** 详情 GET 早就支持 `return_to`，但 12 个操作
  表单一个都不透传，管家每分派一条就被丢回裸列表。九个详情 POST 现在接收并经
  `safe_return_path` 回跳；删除后回来源列表而不是写死的归档视图。
- **「去处理」不再通向空列表。** `_next_step` 手里有 `task_count` 却只在逾期分支
  用了它，到店/离店分支无条件返回任务列表 URL。零开放任务时改去房间详情，按钮
  文案随去向变为「查看房间准备情况」，并说明任务尚未生成——不在这里替业务补造任务。
- `safe_return_path` 支持锚点与自定义兜底。锚点是纯客户端的，scheme 与 netloc
  已在上游被拒，`/employee/x#//evil.com` 的落点仍是 `/employee/x`，不构成开放重定向。
  运营页两处房态表单带上当前 `days` 与房间锚点，兜底改为该房间而不是任务中心。

### 日常页面与维护（UX-05、06、10）

- 无 tab 的客户详情不再摊开全部管理区，统一按 overview 呈现（旧 URL 仍可访问）；
  写操作回到对应标签页，连续审几条记忆不必每次重新滚到记忆区。
- 账号页继承的是登录布局，没有任何导航，此前只能靠浏览器后退键离开；正常已登录
  时给出「返回工作台」。**强制改密走 `auth/change_password.html`，不经过这里**，
  门禁不受影响，并补了守卫测试。改密成功新增一次性提示，机制与 `set_page_error`
  对称：单条短消息、读取即清除，不让签名会话 Cookie 无界增长。
- 知识页四个写操作接收来源并回跳，保留 `query`/`enabled`/`category` 与两组页码。

### 诊断与条件边界（UX-08、09、11）

- **外部调用结果终于有地方看。** 诊断页的「对账轮询已超时」指引到操作记录页查看
  最近调用结果，但那一页只读 `AuditLog`，对 `ExternalRequest` **零处引用**——
  承诺兑现不了，「有审计记录」还会被误当成「有接口结果」。新增按端点汇总的只读
  展示：总次数、失败次数、最近一次的时间与成败。
  按端点聚合而非逐条：生产同一端点已有近百条完全相同的记录（对账轮询每 15 分钟
  一次），逐条列出既看不出问题也翻不完。字段仍按机器码校验，路径带查询串一律
  显示为 `unknown_path`，防止将来有人把客户信息塞进 `path`。
- **审批详情不再被上游拖死。** `get_detail` 原本无条件顺序 await 三个百居易接口
  且无降级，上游一挂，员工连本地审批资料和拒绝入口都打不开。改为逐项降级：三项
  各自失败各自标注，本地资料照常可看，拒绝照常可执行，**只有确认下单不再提供
  入口**——参考数据不完整时绝不用旧房态或旧价格兜底放行，那会造成真实超售或错价。
  已结束的审批完全不请求外部接口。
- 入住密码长度统一到 128（服务层现有约束）。此前模板与路由收 256、服务层拒 128，
  用户要在填完密码、指南和二维码之后才被拒。

### 顺带

- 诊断页「待处理任务」拆成「已到期待处理」与「排期待发」。生产 49 条全部排在
  未来（最早次日、最晚两周后，0 条到期、0 条锁定），一个告警色徽标显示总数会被
  读成积压——与「联网信息查询待确认」同一类问题：只给数字，不说它意味着什么。
  到期数读取失败时退回原告警，不把未知说成排期：失败要往保守方向倒。

## [1.16.0] - 2026-09-08

补上部署前备份，并修掉 v1.15.1 那条办不到的指示，以及它牵出的一个认证缺口。

### 部署前备份（此前 12 个版本裸奔）

- `/opt/yumi-backups/` 最新一份停在 `pre-v1.4.2`（9 月 4 日），此后 v1.5.0 至 v1.15.1
  共 12 个版本没有任何备份，而 v1.13.0 起的部署会跑真实迁移。迁移一旦改错数据，
  没有回退落点。
- `.stage/remote.sh` 在完整性校验之后、任何变更之前新增备份步骤，产出
  `homestay.dump`（pg_dump 自定义格式）、`row-baseline.txt`（逐表精确行数，
  表清单取自当前库，新增表自动纳入）、`git-head.txt`、`api-image.txt`
  和 `RESTORE.md`（两种回滚路径的现成命令）。
- **备份失败就中止部署。** 「失败了也继续」的备份比没有备份更糟，它只提供安全感。
  落盘后用 `pg_restore --list` 复验，因为 pg_dump 中途失败会留下一个大小看着
  正常、实际截断的文件。
- 已在生产实跑验证：产出 36 张表、356 个对象、234662 字节，并**恢复进独立临时库
  逐表比对行数完全一致**后删除该临时库。`--list` 只证明 TOC 可读，不证明数据块完整。
- 备份不自动清理。自动删备份的脚本迟早会在最需要的那天删掉要用的那份。
- `.env` 不进备份：里面是密钥，且部署过程从不改动它。

### 未配置的回调密钥不再能通过认证

- `HostexWebhookService.verify_secret` 只做 `compare_digest` 字符串比较，没有
  「未配置就拒绝」的兜底。**密钥为空串时 `compare_digest("", "")` 为真**——
  发一个空的 Secret 头就能往 `hostex_webhook_events` 写行。
- 影响范围据实说明：中文占位符「未配置」作为 latin-1 的 HTTP 头发不出去，
  那条路打不进来；可直接利用的是空串/空白密钥。当前生产配了真密钥，未受影响。
- 修复后未配置一律拒绝，且与「密钥不对」共用同一句 401 文案——区分开会让
  未认证的调用方靠对比响应探出这台部署有没有配回调。
- 占位值统一为 `domain.runtime_config.UNCONFIGURED_SECRET`，不再各处散落字面量。

### 回调密钥终于清得掉（修 v1.15.1 的假指示）

- v1.15.1 的诊断页写着「清空本系统的回调密钥即可」，但设置页留空表示**保留原值**，
  唯一带清空开关的字段是 `wecom_contact_secret`。那条处理方法指向一个界面上
  不存在的动作——正是这一版本要消除的「只能看着」。
- 新增「明确清除 Webhook Secret」开关。清空机制一般化为 `CLEARABLE_SECRETS` 表，
  每个字段带各自的清空值：`wecom_contact_secret` 清成 `None`（其快照字段是
  `str | None`），`hostex_webhook_secret_token` 清成占位值（其字段是 `str`，
  写 `None` 会在 `validate` 和 `runtime_clients` 的 `.strip()` 上炸）。
- 诊断文案改为指向这个开关，并补上「前往处理」链接。

## [1.15.1] - 2026-09-08

用户问「为什么系统诊断栏显示降级」，页面上确实答不出来——真正的降级项**根本没渲染**。

### 降级原因一度完全不可见（v1.15.0 引入的回归）

- v1.15.0 把健康项 `hostex_webhook_sync` 正名为 `hostex_reconcile`，并新增 `hostex_webhook`，
  却没有同步更新诊断页的标签表 `_CHECK_LABELS`。
- 该表仍写着旧键 `hostex_webhook_sync`，而渲染循环遍历的是**标签表**，遇到
  `key not in health` 就 `continue`。结果：健康检查里有、标签表里没有的项被整项静默丢弃。
- 生产实际是 `hostex_webhook: never_received`（回调配了密钥但一次都没打通，见 `tasks/todo.md`），
  整体状态因此为 `degraded`；但诊断页只显示得出「联网信息查询待确认」和「客户联系同步未配置」——
  两项都不是降级原因。页面显示降级，却看不到任何能解释降级的东西。
- 修法不只是补条目：**循环改为遍历健康检查本身**，缺标签只会退化成显示原始键，
  不会再让任何一项凭空消失。新增回归测试用一个标签表里没有的键来钉住这一点。

### 诊断项从「只能看着」改成能处理

- 每一项异常现在给出三段：这是什么意思、现在该做什么、以及一个可点的去处
  （去处只在确实存在可靠页面时才给，四个链接均已核对为真实路由）。
- 「联网信息查询待确认」加了说明：这是本次启动后尚未发生过真实联网查询，
  是**尚未验证**而不是故障，也不参与降级。此前它长期被误读成异常。
- 非正常但不影响整体状态的项（回调未配置、客户联系同步未配置、联网待确认等）
  显式标注「不影响整体状态」；真正拉低整体状态的项标注「由这一项引起」。
- 这份良性状态表是 `routes/health.py` 判定的副本，副本会漂移，
  因此由 `test_benign_check_states_do_not_degrade` 驱动真实健康服务把两边钉在一起。

## [1.15.0] - 2026-09-08

排查 v1.14.1 时顺带发现的两处观测缺口，与前端方案无关，单独修复。

### 外部调用终于留下可查记录

- `external_requests` 表的注释写着「记录外部接口调用的非敏感结果，便于审计和排障」，但全仓引用只有两处：保留期清理（删除它）和一个测试（构造它）。**没有任何生产代码写入过。** 一张永远为空的表被定期清理了 90 天。
- 于是「这次百居易调用成功了吗」在生产上无从查证。排查 F-05 是否打挂对账时，只能改用「订单写入时间晚于容器启动时间」这类间接推断。
- 百居易客户端现在在三个出口各记一条：成功、业务错误、传输重试耗尽。记录只含提供方、方法、路径、请求编号、业务码和成败——不含参数、请求体或响应正文，避免为日期、房号甚至令牌开一条新的外泄路径。
- 记录走独立短事务，与业务事务分开：不会因为它写不进去而回滚一次成功的同步，也不会反过来被业务回滚抹掉——排障时最想看的恰恰是失败那一次。记录器自身出错只记警告，绝不影响业务调用。

### 健康检查不再用对账心跳替 Webhook 作证

- `hostex_webhook_sync` 名字说的是回调，实际读的是 `hostex_sync_last_success`——一个由**对账轮询**刷新、并且在进程启动时就被直接赋成当前时间的心跳。结果是：生产上 `hostex_webhook_events` 表 0 行、153 笔订单全部来自轮询，而这一项一直报 `ok`。
- 按实际测量对象正名为 `hostex_reconcile`，另加真正由 Webhook 事件驱动的 `hostex_webhook`。后者初始化为空而不是启动时间，从未收到过就报 `never_received`。
- 未配置回调密钥时报 `not_configured` 且不参与降级，与 `wecom_contact_sync` 的既有约定一致；配置了却从未收到才降级——那才是真正需要有人去查的故障。

### 边界

- 未向 Webhook 端点发送任何 POST：任何 POST 都是写入尝试，可能造出垃圾事件行。用 `GET` 探到 405 已足以证明路径已挂载。
- 未读取回调密钥的值，只判断它是否仍是默认占位符。
- 百居易侧是否配置了回调地址需要登录其后台核对，不在本次范围。

## [1.14.1] - 2026-09-08

### 工作台与房间视图对来源可信度给出一致结论

- 工作台的「先处理」快照漏传 `source_synced_at`，默认 `None` 会把每间房都判成来源过期：整列变成「先确认百居易实时房态」且没有任何可点的去向，而同一时刻房间视图显示同步正常、下一步是真实原因。同一份数据在两个视图里给出互相矛盾的可信度。
- 生产实测发现：房间视图「最近同步 14:46」、无过期告警，工作台却七间房全是「先确认百居易实时房态」。两个视图现在取同一个同步时间。

## [1.14.0] - 2026-09-08

来源：[2026-09-08 前端交互与运营功能精简方案](docs/reviews/2026-09-08_frontend-operations-optimization-report.md)。
报告基线为 `3c9d963` / 1.11.1，实施前已在 1.13.0 上按其符号逐条复核，确认所列问题仍然成立。
用户确认整合日常入口；保洁「任务完成」与「房间可入住」的业务合并按建议推迟，待可靠性问题沉淀后再议。

### 确认文案跟随实际动作

- 同一个表单挂着归档、分派、取消、永久删除四个按钮，后三个靠 `formaction` 改写目标，而表单级 `data-confirm` 是写给默认动作（归档）的。点「分派勾选的任务」弹出的是「确定把勾选的任务移入归档吗」。1.11.1 只给带手输确认的按钮做了跳过，没有处理问题的一般形式。
- 现在确认文案跟随提交者：自带文案就用它，支持 `{n}` 与 `{employee}` 占位符按本次提交填入真实数值；改写了 `formaction` 却没带文案的宁可不问，也不问一件它不做的事；默认按钮仍读表单级文案。
- 修正选择区自相矛盾的说明。「只作用于它接受的那部分」与「混入不合格的条目会整批拒绝」不可能同时为真，实际行为是整批拒绝。

### 保留工作上下文

- 任务详情的返回此前固定指向默认列表：从「某房间待确认队列第 2 页」进来，处理完被丢回全部待办，等于每处理一条就把筛选重设一遍。列表链接改为携带来源，详情按 `safe_return_path` 校验后返回，站外与非 `/employee/` 路径一律落到兜底地址。
- 筛选不再「有条件就自动展开」。手机上从房间卡进来只加了一个房源条件，却要先翻过七类控件才看得到第一条任务；生效条件由摘要行说明，展开与否交给用户。

### 四个并列入口收敛为工作台

- 总览、待我关注、房态与运营是三个并列导航项，任务中心是第四个，同一件工作散在里面。侧边栏现在只留一个「工作台」，今天／房间／全部任务由页内标签切换。
- 房间视图仍然就是既有的 `/employee/admin/operations`：不新建页面、不复制第四套待办数据，三个旧 URL 全部保持可达。

### 「下一步」带上真正能点的去向

- 服务早已能区分逾期、退房周转、入住准备与维修，页面却只展示文字，常驻的直接操作反而是房态下拉框。读完还得回列表重新筛选才能动手。
- 现在同一组分支同时产出原因与去向：逾期带房间加逾期条件，维修去房源页，有开放任务进该房间列表。文字与去向分开计算就会漂移，因此一次算出。同步不可信时没有能真正解决它的入口，返回空值，页面不渲染看起来能点的空按钮。
- 工作台「今天」新增「先处理」清单，直接用运营快照里已排好风险序的房间；读取失败安静降级为空列表。

### 材料缺失提前说明

- 待检查阶段原本同时摆着保存清单、上传照片、确认可入住、标记完成四个并列提交入口，执行员工得先点「确认可入住」被拒，才知道少了哪一项。现在在动作之前逐项列出还缺什么。
- 判据与 `RoomReadinessService.mark_ready` 的守卫共用同一份必填清单定义，避免页面说齐了而提交仍被拒绝。提示不替代校验，服务端仍独立复核。

### 边界

- 不改提交语义、权限、CSRF、审计与凭证安全门；不引入 SPA、图表库或工作流引擎。
- 保洁的「任务完成」与「房间可入住」未合并：两者语义不同，一个改房态并触发凭证评估，一个推进任务状态。
- 不迁移数据库枚举，视图分组只是展示，真实业务状态不变。

## [1.13.0] - 2026-09-08

来源：[2026-09-08 代码审查交接报告](docs/reviews/2026-09-08_code-review-handoff-report.md) 的 A 单元（F-01、F-07）。
报告基线为 1.11.0，实施前已在 1.12.0 上重跑其离线复现脚本确认九项全部仍然成立。

### 删除照片不再早于数据库提交（F-01）

- 单条删除、批量删除与保留期自动清理三个入口，原先都是先删磁盘照片再另开事务删数据库行。注释把顺序说成刻意的：「反过来失败只留下一条指向缺失文件的记录，可以修复」。那句话对记录成立，对照片不成立——提交一旦失败，数据库回滚而照片已经没了，没有备份就无法重建原图；批量删除还可能只删掉一部分文件就中断。
- 现在删库、审计与照片清理登记在同一事务提交，照片由既有 worker 在提交之后幂等删除。不需要迁移也不需要新表：`jobs` 表本就是通用的 `job_type` + JSON payload + `dedupe_key`，重试与失败状态都现成；存储的 `delete` 本身就是 `unlink(missing_ok=True)`，文件不存在即视为完成。任一编号删除失败让整条任务重试，保留可观测的待清理记录。

### 删除的任务不再被同步造回来（F-07）

- 永久删除会把去重依据（`turnover:{房间}:{服务日}`）连同任务行一起删掉，而创建侧只查现存任务，于是同一订单再同步一次就重新生成待分派任务，已经处理完并被清理的历史工作回流到运营待办。
- 新增 `purged_task_marks` 墓碑表（迁移 `0025_purged_task_marks`），只保留被删任务的去重键与删除时间，不含正文、照片或客户身份。创建前先查墓碑；命中则不重建，且不视为错误——同步照常继续。
- 墓碑保留 180 天后失效，过期即允许重建。否则删过一次就等于永久封禁某个房间的某一天，一年后真的需要清扫也不会生成。
- 沿用现有去重键作为身份，不引入第二套概念。现有审计日志不满足这个用途：`business_task_purged` 只记 `task_ids`，任务行删除后无法反查房间与服务日期。

### 订单、房态与凭证闭环（F-02、F-03、F-05、F-06、F-08、F-09）

- **凭证不再发给退房客人（F-02）**。周转保洁任务绑定的是**退房**订单，房间就绪后却把它直接交给凭证评估：日期不符会被安全门拒绝，而当天真有新客人时也没人替他重新选订单。现在按「同房间 + 业务当日入住」重新选订单，再走全部现有安全门。没有当日入住订单是正常情况，安静结束不报错；出现多个候选则不猜，建一条人工复核任务——把门锁密码发错人的代价远高于晚一点发。
- **异步失败回执回写凭证状态（F-03）**。平台接口受理只证明服务端收下了，不证明客人收到。失败回执此前没有任何地方与凭证部件关联，部件和整组都停在 `SENT`，人工也拿不到待办。现在按外部消息编号锁定部件，失败部件与整组转人工，尚未开始的同组部件一并暂停，并建一条去重的人工任务。已成功的部件不重放，重复回执不重复处理，未知编号不误改其他消息。
- **对账覆盖仍在住的长住单（F-05）**。只按入住日过滤会漏掉三天前入住、两天后离店的订单：漏了 Webhook 就补不回来，而心跳照常成功，运营页把这间房显示成空闲。现在发两次独立查询——入住日区间与退房日区间——再按订单编号合并。不用一次组合查询，是因为组合条件是 AND 还是 OR 尚未经过授权的契约验证；也不只用退房日，那会漏掉退房远在窗口之后的长住单。分页与传输失败都会抛出，因此心跳不会在数据不全时刷新。
- **订单改期后作废旧的周转计划（F-06）**。改期既不是取消也没到窗口期，此前根本进不了生命周期候选，于是旧日和新日两个待分派任务同时存在，可能出现错误日期的清扫。现在按「任务的房间或服务日期与来源订单当前值不一致」收进候选，且只作废没有任何执行痕迹的系统任务；已分派、有清单或有现场照片的一律保留交人工。
- **凭证安全门改用武汉业务日（F-08）**。默认取 `date.today`，容器按 UTC 运行时武汉 8 月 2 日凌晨 1 点在 UTC 还是 8 月 1 日，安全门会把当天入住的客人判成「不是入住日」而拒绝发送。文档字符串写的是「武汉业务日期」，默认值却取系统日期。
- **worker 超时前先落业务状态（F-09）**。`credential_send_part` 不可重放：外部调用可能已经发出，结果未知。原先只把 Job 置为 `FAILED` 并清空载荷，凭证部件仍停在 `PENDING`、人工任务为 0——系统不再自动处理，却没有任何人能接手。现在在清空载荷之前取得凭证关联（`part_id` 只存在于 payload 里），同一事务把部件与整组落成待人工，并登记去重的人工复核。结果未知时不自动重发密码或二维码。

### 排队中的回复出站前复核时效（F-04）

- 生成阶段已有过时判定，但它管不到排队期间：入队时有效的回复，可能在真正出站前已经被新的客人消息或人工回复取代，发出去就会打断人工、或回答一个已经不成立的问题。现在实际发送前复用同一套排序口径再判一次，过时则记可审计的跳过原因，不记作已发送。
- 只有带来源客人消息的回复参与判定，重试、客诉投递与员工通知各有自己的幂等与审批边界。外部请求一旦发起无法撤回，这道校验只覆盖已经入库的活动，不承诺消除全部并发时间窗。

### 边界

- 不建立通用跨资源事务框架，复用既有 `jobs` 表。
- `create_turnover` 与 `BusinessTaskService.create_turnover` 返回类型放宽为 `BusinessTask | None`，`None` 表示来源已被删除且仍在保留期内。
- 历史数据处置（已缺照片的任务、疑似删除后重建的任务）不在本次范围，需要独立方案与授权。

## [1.12.0] - 2026-09-08

### 「待我关注」的每一项都可处置

- 入住提醒此前没有任何出口。一旦转入人工跟进，它是否还显示在「待我关注」取决于**那条派生任务是否仍处于待确认**——而管理员推进它、取消它、或把它归档后永久删除，都是完全合法的操作。任务一旦离开待确认或被删除，提醒就重新涌回列表，且 `ReminderStatus` 里没有「已处理」这一档，于是再也下不去。生产上 419 项待关注里有 403 项是这样的提醒，占 96%。
- 排除判据交还给提醒自己：新增 `RESOLVED` 状态与管理员「标记已处理」批量入口，直接开在关注页上。同时不再要求派生任务停在特定状态——判据应当是「这条提醒是否已被移交」，而不是「移交出去的那条任务此刻停在哪一格」。
- 提醒的「立即处理」不再指向任务列表。提醒不是业务任务，那个列表里根本没有它；现在它在被列出的地方就地了结。凭证投递同理，改为指向房源的凭证页签。
- 业务任务的分组入口补上了「待确认」条件。此前只按房源筛，点「10 项业务任务待确认」会落到该房源的全部开放任务上，看到的是一堆已分派任务。

### 任务列表的每个状态都有一个真能落下去的动作

- 新增批量取消。此前可批量的只有分派（要求待确认或待分派且信息齐全）和归档（要求终态），中间三档——已分派、进行中、待检查——一个动作都不接受，于是管理员点进任务列表看到全选框却一条都选不了。生产上默认队列 117 条待办里可勾选数为 0。
- 不放宽任何规则。取消是状态机里唯一一条从每个开放态都通向终点、且只有管理员走得通的路径，批量取消逐条复用既有的状态迁移，权限、状态校验与审计一律照旧。「可取消」直接取「非终态」，复用既有的终态定义，不新造第二份状态清单。
- 勾选框因此对所有任务出现：开放态可取消，终态可归档，已归档可永久删除，没有哪一格是死的。
- 取消不可逆（状态机里 `CANCELLED` 没有出路），因此与永久删除同样要求手输条数，而不是弹框点确定。手输确认的后果说明改由按钮提供——同一套确认现在服务于删除和取消两种动作，措辞写死在脚本里，另一种动作就会读到一句与它无关的警告。

### 「需人工关注」只有一个数

- 总览与关注页此前各写一套查询，谓词有三处分歧：总览不计待确认的业务任务、不排除已移交给任务的提醒、漏掉客诉的退回状态。实测同一时刻总览 408、关注页 419，差值 11 恰好等于「16 条待确认任务 − 5 条重复计入的提醒」。处理掉一条任务，关注页减一而总览纹丝不动。
- 总览改为直接复用关注页的查询，不再自行 COUNT。新增测试在真实数据库上同时算出两侧并断言相等，把未来再次分叉变成 CI 失败而不是又一次静默不一致。

### 边界

- 不改状态机的迁移规则、权限判断、归档语义与保留期。
- 不新增数据库迁移：提醒状态列是 `VARCHAR(32)` 且无 CHECK 约束，已实测确认。
- 标记提醒为已处理只改提醒自身状态，不动订单、任务、客户资料或凭证。

## [1.11.1] - 2026-09-08

### 后台视觉打磨

- 中性色统一到同一个 slate 家族。`--line` 原为 gray-200（`#e5e7eb`，蓝偏 +6），比它上一档 `--line-strong`（+22）还要灰，色阶不单调；换成 slate-200 后「页面 +6 → 线 +14 → 强线 +22」逐级递进。这一改顺带消掉了徽标里重复写死的同值。
- 补齐四个缺失的语义色 token：`--surface-sunk` 与三档正文墨色 `--ink-strong` / `--ink-mid` / `--ink-soft`。这些值此前共 60 处直接写在规则里，同一个语义散落成字面量，改一次要翻遍整张表，标题、标签和次级链接各自该取哪一档也无从判断。
- 面板内的卡片不再是「白卡套白卡」。`.candidate-card`、`.entry-card`、`.message-item`、`.list-card` 多数长在 `.panel` 里，而面板本身就是白底加同款边框和圆角，两层一模一样的容器叠起来会把层级抹平。父级是面板时改用沉降底色，靠明度而不是又一圈边框区分；独立铺在页面背景上的卡片（如运营页的房间卡）不受影响。
- 登录卡只声明一次高度。此前同时有 1px 边框和一道几乎看不见的柔和阴影，改为去掉边框、给一道带偏移和模糊的真实阴影。
- 修正两处近乎不可见的浅色：表格行悬停原为 `#fafcff`，与白底只差约 1% 明度，扫 50 行任务时等于没有反馈；空状态底色同理。两处都改用沉降色。

### 边界

- 不改结构、不改交互、不改文案，不新增字体、依赖或暗色模式。对比度未受影响，正文与占位符在改动前后均高于 4.5:1。

## [1.11.0] - 2026-09-08

### 任务中心区分「当前队列」与「附加筛选」

- 队列高亮此前由归档、逾期、状态各自判断，`?archived=true&status_filter=pending_confirmation` 会让「已归档」和「待确认」同时亮起，读屏也会读到两个当前位置。现在由服务端算出唯一队列（归档 > 逾期 > 状态 > 开放），高亮、标题和清除入口都从这一个答案出发。
- 页面主标题与浏览器标题跟随队列。已归档页此前仍自称「全部待办」，看不出自己在哪。管理员与员工两种视野各有称呼，员工不会把「逾期」误读成全公司的逾期。
- 高级筛选的展开条件改为「队列之外是否还有条件」。此前点一下「待分派」就算加了筛选，整套高级表单会展开在用户面前。真有附加条件时给一行摘要，写明加了哪几项，并提供一键退回纯队列。
- 状态下拉的占位项由「全部开放状态」改为「不限状态」。在已归档队列里前者是错的。

### 房态时间轴不再挤压，也不再把「无记录」说成「空闲」

- 日期格此前允许被压到任意宽度，且只有最窄断点设了下限，桌面恰好漏在外面：14 天时间轴塞进半宽卡片时每格只剩二十几像素，而「退房 1」这类标签本身就要四十多像素，相邻信息直接重叠。现在所有断点都保证最小宽度，装不下就横向滚动；跨度超过 7 天时房间卡片改为整行，滚动退回兜底而非常态。
- 时间轴会横向滚动，因此自身可被键盘聚焦，否则键盘用户看不到后面的日子。
- 百居易同步过期时，本地没有记录只能说成「无记录」，不能逐日写「空闲」。页面顶部已经声明房态待确认，时间轴却用最具体的形式把「我们不知道」讲成「我们确定这天没人」。时间轴上方同时标注这是上次同步记录。同步可信时「空闲」仍是事实陈述，不降级。

### 稳定房间展开后能看到未来安排

- 稳定房间此前只有房名、准备状态和一个房态下拉，而页面顶部提供「未来 3／7／14 天」入口，展开等于什么也没多看到。现在展开后有开放任务（可跳转到该房源的任务列表）、下次入住、下次退房和对应时间轴。所需数据本就在快照里，只是页面没有使用。

### 运营准备度只有一套颜色语义

- `operational_status` 此前有三套互不相同的映射：房源列表桌面按可入住取绿或黄、手机固定用蓝、房源详情又一套，维修在任何一处都不是风险色。同一间房在两块屏幕上是两种颜色，颜色也就不再有含义。三处统一到同一个徽标宏：维修是风险，可入住是达标，其余是中性事实，颜色之外始终带明确文字。

### 边界

- 不改队列本身的筛选结果、权限判断与任务生成规则；队列判定只决定高亮、标题和展开行为。
- 不改房态数据来源与同步判定，只改它在页面上的说法。
- 不新增动画、图表、字体依赖或组件库。

## [1.10.1] - 2026-09-08

### 批量操作按用户看见的范围执行

- 任务列表的桌面表格与手机卡片各渲染一份同名勾选框，两份始终都在 DOM 里。「全选本页」无差别勾上两份，提交的编号翻倍；取消可见的那一份时，隐藏副本仍带着该任务进入归档或永久删除——页面显示未勾选，任务却被处理了。现在同一任务只保留当前可见的那一份可提交，另一份同步状态并禁用；切换屏幕宽度时选择被继承而不是复制。禁用控件不进入表单数据，因此脚本不可用时同样只提交可见的选择。
- 手输确认的条数改为按任务编号去重统计。此前数字是两套布局的总和，勾 2 条会要求输入 4。
- 永久删除不再沿用归档表单「可在「已归档」中恢复」的确认文案。该文案在专属确认之后弹出，等于用一句「可以恢复」盖住了不可逆的真相。带专属确认的按钮不再触发表单级确认。
- 「归档当前筛选的终态任务」此前只提交状态、类型和房源，丢掉服务日期与执行员工：按日期加员工筛完再归档，会连别的日期、别人的任务一起归掉，页面上没有任何地方提示范围变宽了。现在列表用到的每个条件都原样传到服务端。
- 「只看逾期」无法在归档查询里表达，因此该筛选生效时不再提供归档入口，服务端收到该条件也直接拒绝。宁可不给这个操作，也不能按更宽的范围执行。
- 归档按钮上方展示本次归档的具体范围，条件在点击之前就写在页面上，而不只藏在确认框里。

### 修复分派表单默认选错房间

- 任务详情的「确认并分派」不回填任务已有的房间与执行员工，浏览器因此默认选中选项表第一项。管理员以为自己只是确认原样，提交后任务被改到了另一个房间。现在已有值回填，缺失时显示占位项并要求主动选择，不给默认值。

### 修复手机端表格整块消失

- 客户住宿记录与房态「紧凑总览」使用了手机端整体隐藏的 `.responsive-table`，却没有配套的手机卡片列表：390px 宽度下 DOM 里有完整数据，可见记录为 0，页面既不报错也不显示空状态。两处改用可横向滚动的 `.table-scroll`。新增结构性测试扫描全部模板，禁止再出现没有手机端替代内容的 `.responsive-table`。

### 边界

- 不改权限判断、状态机、归档语义与保留期。可勾选的任务集合、批量操作的服务端校验规则均未放宽。
- 不改列表查询本身，只让批量操作使用与列表相同的条件。

## [1.10.0] - 2026-09-08

### 任务支持批量分派

- 任务列表可勾选多条一次分派给同一名执行员工。生产上 89 条待分派任务的房间与日期全部齐全、唯一缺的是执行人，而此前每条都要打开详情、逐项确认再提交返回，仅为填一个字段就需要数百次交互。
- 沿用每条任务自身的房间与服务日期，只写入执行人。这些任务分属不同房间和日期，批量覆盖这两项才是危险操作。
- 逐条复用既有分派服务，状态机、权限校验与审计一律不绕过。校验先整体完成再执行，避免前几条已改状态才发现后面某条不合格。
- 缺房间或服务日期、状态不符的任务整批拒绝并列出具体编号；未选择员工时拒绝。
- 勾选框的出现条件按批量动作区分：可分派要求待确认或待分派且房间与日期齐全，可归档要求终态，两者都不满足时不给勾选。操作区显示本页可分派与可归档的数量。

### 边界

- 不改状态机、权限判断或任务生成规则，不提供自动分派。
- 不批量修改房间、服务日期或任务类型。

## [1.9.0] - 2026-09-08

### 已归档任务支持批量永久删除

- 「已归档」视图提供批量永久删除。1.8.0 只做了单条删除，导致清理存量归档任务只能逐条点击或等待 180 天保留期，实际不可用。
- 用勾选而不是筛选。归档可以按筛选批量执行是因为它可逆；删除不可逆，必须让操作者看着具体条目再动手，因此一次最多处理当页数量。
- 提交时要求手动输入删除条数，数字与勾选数不一致即拒绝。这挡住的是「习惯性点确定」这一整类事故：数字对不上说明操作者没有真正看过数量。脚本不可用时确认数为 0，服务端同样拒绝。
- 服务端逐条复核归档状态，混入未归档任务时拒绝整批并列出具体编号。校验在删除磁盘文件之前完成：先删文件再发现某条不该删，照片已经找不回来。删除后再复核一次，因为校验与删除之间隔着文件删除，期间状态可能被其他会话改变。
- 归档按钮与永久删除按钮按视图分开：默认视图只给归档，已归档视图只给删除。

### 边界

- 不改归档语义、保留期或权限判断。未归档任务在任何路径下都不会被删除。
- 不提供按筛选条件的批量删除，也不提供跨页全选删除。

## [1.8.0] - 2026-09-08

### 已归档任务可彻底删除

- 任务详情页对已归档任务提供「永久删除」。归档是删除的前置门槛，列表页不提供直接删除路径。仓储内重新读取并校验归档状态，不信任调用方传入的判断。
- 删除会一并清除现场照片。附件行由外键级联删除，但磁盘文件必须显式删除，否则会留下永远无人认领的孤儿照片。删除顺序为先文件后数据库行：反过来一旦文件删除失败，照片就再也没有记录指向它；当前顺序失败时只留下一条指向缺失文件的记录，可以修复。
- 审计记录保留并新增 `business_task_purged`，记录任务状态、归档时间与附件数量。审计本就应当比它描述的实体活得久。
- 不提供批量彻底删除。归档可批量因为可逆，删除不可逆，一次误操作没有任何补救。

### 归档任务在 180 天后自动清理

- 每日清理循环新增一项：删除归档超过 180 天的任务及其照片，删除数量计入既有日志行。此前 `business_tasks` 完全不在任何清理范围内，归档任务会永久堆积。
- 只删已归档任务。未归档的任务无论多旧都不会被自动删除——自动删掉还没人处理的活，比留着它危险得多。
- 该清理与既有 `purge` 分开实现：它是唯一带磁盘副作用的清理，混入纯 SQL 语句表会让边界难以验证。
- 新增测试逐条锁定边界：归档满 181 天的被删除，归档仅 179 天的保留，从未归档的保留，磁盘照片确实被删除。

### 边界

- 不改归档语义、权限判断或状态机。未归档任务在任何路径下都不会被删除。

## [1.7.1] - 2026-09-06

### 失败后回到提交时所在的筛选视图

- 1.7.0 的回跳路径取自 `Referer`，而本应用响应头含 `referrer-policy: no-referrer`，浏览器恒不发送该请求头。生产实测每次失败都落到默认任务列表，丢掉当前筛选：在「已归档」视图提交失败后会被送回开放任务列表。回跳路径改由表单显式携带，仍只接受本站 `/employee/` 下的相对路径。
- 对应的集成测试此前手工设置了 `referer` 请求头，测的是生产中不可能出现的情况，因此没能发现该缺陷。新增测试在不带任何 Referer 的条件下断言回跳目标，并单独锁定跨站与非 `/employee/` 路径一律落到兜底地址。

## [1.7.0] - 2026-09-06

### 操作失败回到原页面并说明原因

- 表单提交失败此前返回 `{"detail":"任务操作未完成"}` 的 JSON：整个后台界面消失，用户只能靠浏览器后退，而服务里刻意写给使用者的拒绝原因被完全丢弃。现在业务拒绝会带着原因重定向回原页面，以既有的红色提示条展示；提示读取即清除，刷新不会重复出现，也不会重复提交。接口调用继续返回 JSON。
- 新增 `OperationRefused` 领域异常，作为「消息可安全展示」的唯一标记。归档的三处拒绝改用该类型，其中「以下仍在处理中：[编号]」现在能真正到达用户。
- 安全边界不变且被更严格地锁定：未知异常仍只记录类型与追踪号，页面仅得到通用文案。新增测试断言含连接串的异常原文绝不出现在响应或后续页面上。
- 回跳地址只接受同源的 `/employee/` 路径，其余一律落到任务中心，避免开放重定向。

### 错误处理机制收敛

- 任务与房源页共用同一套异常映射机制，三段用户可见文案仍由各自传入：统一机制不应顺手改写既有措辞。
- 客户管理的三个领域异常改为可展示类型并各自携带 403/404/409。它们原本就是本项目里唯一做对这件事的实现，本次是把任务与房源提升到同一做法，而不是把它们拉平。
- 新增结构性测试锁定真实应用已注册业务拒绝处理器。处理器只在应用入口注册，而集成测试各自搭建裸应用，漏注册时业务拒绝会冒泡成 500 且桩化测试发现不了。

### 边界

- 不改权限判断、状态机、归档语义，不改 401 未登录跳转与既有 403/404 映射。

## [1.6.2] - 2026-09-05

### 只对可归档的任务提供勾选

- 仍在处理中的任务不再渲染勾选框。此前整页任务都可勾选，而服务端只接受终态任务并对混入开放态的选择整批拒绝，因此「全选本页」在多数筛选条件下必然失败。规则本身正确，错在页面引导用户走进注定被拒的操作。
- 提交区显示「本页 N 条可归档」，无可归档项时不再给出提交按钮。确认文案同步更新：它此前描述的「勾选中若含仍在处理的任务」在界面上已无法发生。
- 可归档状态抽出为 `ARCHIVABLE_TASK_STATUSES` 单一定义，仓储校验、列表勾选与详情页归档入口共用。此前仓储有一份、任务详情模板硬编码一份，两处漂移会让页面给出服务端必然拒绝的入口。新增测试锁定唯一定义并禁止模板再写硬编码副本。

### 边界

- 不改归档语义与权限判断。服务端「混入开放态整批拒绝」的校验保留为第二道防线，只是界面上不再可能触发。

## [1.6.1] - 2026-09-05

### 修复房态直改始终返回「房态服务未就绪」

- 管理员直接设定房态自 1.5.0 起从未可用，提交后固定返回 503。路由读取 `app.state.room_readiness_service`，而该属性在整个代码库中从未被赋值，唯一的引用就是路由里那次带默认值的读取。1.6.0 把入口加到运营页只是让这个缺陷更容易被触发，房源详情页的入口同样一直无效。现补上会话门面并在装配处赋值。
- 新增结构性回归测试：扫描全部路由文件中对 `app.state` 的读取，逐个核对装配处是否真正赋值。路由经 `app.state` 取服务，该处静态类型是松的；装配又发生在应用 lifespan 内，而集成测试用桩搭建裸应用、从不执行真实 lifespan，因此这类缺陷既不会被 mypy 发现也不会被集成测试发现，只会在真实请求时返回 503 或抛 AttributeError。两处时钟注入缝有 callable 判断并回落到真实 UTC 时钟，已显式排除并记录理由。

### 边界

- 只补装配与测试，不改房态领域规则、权限判断或页面。人工覆盖审计 `room_status_manual_override` 行为不变。

## [1.6.0] - 2026-09-05

### 房态可在运营页就地修改

- 房态与运营页的每张房间卡片和稳定房间列表内嵌房态下拉与保存按钮，直接提交到既有的房源房态接口。此前修改一次房态需要「运营页 → 点房间标题 → 房源详情 → 提交 → 返回」四步往返，而这是日常运营中最频繁的动作之一。房源详情页的入口保留，两处并存。
- 就地修改沿用房源写操作家族的一次性令牌，逐房间绑定，跨房间重放仍会失败；二次确认与 `room_status_manual_override` 审计不变。实测生产令牌水位：全局 18/800，单作用域上限 8，本次新增约 7 个作用域各至多 8 个，额度充足。

### 任务列表支持勾选归档

- 桌面表格新增勾选列，移动卡片同步提供勾选，表头「全选本页」在脚本可用时生效；脚本缺失时逐条勾选与提交仍然可用。
- 勾选中混入仍在处理的任务时整批拒绝并列出具体编号，不静默跳过。静默跳过会让人以为全部归档成功而实际漏了几条，比直接报错更难发现。
- 保留既有的按筛选批量归档：勾选用于精确挑选当页任务，筛选用于一次扫掉全部符合条件的历史任务，两者覆盖不同场景。
- 批量与勾选归档的一次性令牌改为只对管理员签发。此前对普通员工也签发，而员工列表并不渲染这些入口，白白占用令牌额度。

### 边界

- 不新增数据库迁移、依赖或运行配置，不改变归档语义、房态领域规则与权限判断。
- 不提供跨页全选，按筛选批量归档已覆盖该场景。

## [1.5.2] - 2026-09-05

### 修复归档视图恒为空

- 只勾选「查看已归档」而不另选状态时，归档列表永远显示为空。归档只接受终态任务，而列表在未指定状态时会附加「仅开放态」条件，两者相与恒不成立。该组合正是最自然的用法，因此归档后的任务实际上无处查看。归档视图不再叠加开放态限制。
- 「已归档」从默认折叠的高级筛选提升为快速队列页签，仅管理员可见。此前入口藏在折叠面板内，归档完成后无法直观找到任务去向。

### 边界

- 只改列表查询条件与入口位置，不改归档规则、权限判断或已归档数据。开放队列仍保持四项，「已归档」属于归档维度而非开放队列。

## [1.5.1] - 2026-09-05

### 修复归档与审批拒绝在生产不可用

- 1.5.0 的批量归档、单条归档、恢复和审批拒绝在生产上全部抛 `AttributeError`，被错误处理压成 409。路由从 `app.state` 取到的是会话门面 `SessionTaskPageService` 与 `SessionApprovalPageService`，而 1.5.0 只给内层服务和 Port 协议加了方法，漏改了这两个门面。补齐门面的 `archive`、`restore`、`archive_filtered` 与 `reject`。
- 新增结构性回归测试，逐个方法名比对路由 Port 与生产会话门面。路由通过 `app.state` 获取服务，该处静态类型是松的，因此这类「Port 与内层服务都加了、门面漏加」的缺陷不会被 mypy 发现，只会在真实请求时暴露。该测试覆盖任务、审批、房源和客户四个门面，堵的是整类问题而不是这四个方法。审批拒绝正是由这个测试发现的：生产当时没有待处理审批，页面验收无法触发它。

### 边界

- 只补门面委托与测试，不改业务规则、权限判断、数据结构或页面。

## [1.5.0] - 2026-09-05

### 任务闭环补齐执行入口

- 任务详情页在待检查状态下新增「标记任务完成」。此前模板只在 `not is_admin` 时展示现场证据与完成入口，而服务端判断的是「是否本任务执行员工」，两者维度不同：管理员被指派任务后既看不到清单与照片入口，也看不到完成入口，任务只能停在待检查。生产环境唯一员工即管理员，因此任务从未走完过一次完整流程。入口条件改为「执行人或管理员」，与服务端规则逐字对齐。

### 管理员可直接设置房态

- 房源详情页的运营概览页签新增房态设置。`RoomOperationalStatus` 定义了六个状态，但全仓只有「确认可入住」与「撤回可入住」两处写入点，清洁中、在住和维修中永远无法被设置 —— 总览页的状态点样式与运营页的维修徽标在展示不可能出现的状态。现在管理员可直接设置全部六个状态，用于热水器损坏停售、临时占用等证据流程覆盖不到的情况。
- 人工设置额外记录一条 `room_status_manual_override` 审计，声明该次变更未经清单与照片证据。可入住是向客人发送门锁密码的前置条件，人工直改绕过了证据要求；单独留痕才能在事后区分证据路径与人工路径。
- 仓储既有守卫保持不变：在住或维修中的房间仍不能直接置为可入住。

### 审批可以拒绝

- 审批详情页新增「拒绝预订」，必须填写拒绝原因，原因写入审计。此前审批只有确认没有拒绝，不该接的预订只能一直挂在待办里。仅待确认、存在冲突和需要复核三种状态可拒绝，正在创建订单的审批不提供入口。

### 失效任务可归档

- 任务新增软归档：`business_tasks` 增加 `archived_at` 与 `archived_by_employee_id`，默认列表排除已归档，新增「查看已归档」筛选，归档任务可随时恢复。此前系统没有任何删除或归档能力，生产已累积 394 条失效任务且只会持续增长。
- 只有已完成、已取消和已失效可归档，开放中的任务被拒绝，避免归档被用来隐藏未完成的工作。
- 批量归档以当前筛选条件为选择范围，不引入多选提交：列表页既有筛选器本身就是最自然的选择方式，而为数百条记录新建一套复选框与批量提交系统并不划算。批量只写一条含数量与筛选条件的汇总审计，不为每条任务各写一条。
- 归档不参与任务状态机，仅影响列表可见性，任务状态原样保留，因此恢复是无损的。

### 边界

- 归档为软归档，不提供硬删除。不回填历史数据，不改变任务失效规则、状态机、`mark_ready` 的证据要求，也不改变凭证投递的安全门顺序。
- 不改变页面地址、表单字段名称、CSRF 与二次确认逻辑，不新增依赖或运行配置。

## [1.4.2] - 2026-09-04

### 修复任务无法完成：现场证据入口按角色判断

- 任务详情页的房间检查清单、现场照片上传和确认房间可入住三个入口，此前以
  「当前员工不是管理员」作为显示条件。服务端的真实规则与角色无关：
  `require_evidence_editor` 和 `room_readiness_service.mark_ready` 都只要求
  「必须是该任务的执行员工」。模板判断的是角色，服务端判断的是执行人身份，
  两者是正交的维度 —— 一名管理员完全可以是某个任务的执行人。
- 后果是：在员工全部具备管理员角色的部署下，任务被分派后再也找不到提交现场
  证据的入口，只能停在已分派状态直到窗口关闭而失效。房间因此永远到不了可入住，
  依赖该状态的入住凭证投递也随之无法触发。
- 现改为按服务端同一判据显示，即当前员工是否为本任务的执行员工。该判断由路由
  计算后传入模板，与 `require_evidence_editor` 的语义保持单一来源。
- 这是严格增量的修正：原本可见这三个入口的执行员工不受影响，不是执行人的管理员
  仍然看不到，只有「作为执行人的管理员」拿回了服务端本就允许的操作。

### 回归保护

- 补充三项任务详情页的权限门控测试：执行人是管理员时必须出现清单与照片入口、
  必须出现确认可入住入口，以及管理员不是执行人时三个入口必须保持隐藏。第三项在
  修复前后均为通过，用于防止把角色判断直接删除而把权限放得过宽。
- 服务层的可入住闭环此前已有测试覆盖并持续通过，缺口只存在于页面是否暴露该路径，
  本次补上的正是这一层。

### 边界

- 未改变任何服务端权限规则、任务状态机、清单与照片的前置条件，也未改变确认可入住
  对清单完整和至少一张现场照片的要求。
- 未改变分派、取消任务、撤回可入住三处仍应由管理员执行的判断，任务列表页按角色
  区分可见范围的行为同样保持不变。
- 未新增数据库迁移、依赖或运行配置，未改变页面地址、表单字段、提交目标与二次确认
  逻辑，也未对历史已失效任务做任何回填。

## [1.4.1] - 2026-09-04

### 后台视觉系统统一

- 表现层取值收敛为设计令牌：16 种字号并为 7 级字阶，5 种圆角并为标记、控件、卡片三档，新增版面尺度与层级令牌。此前同一角色的文字在不同页面用 `.66/.68/.70/.72rem` 等多种字号，卡片圆角在 4px 到 8px 之间随手取值，界面缺少可复用的尺度。
- 层级只声明一次：面板用发丝线加页面底色分隔，阴影只留给抽屉、浮层与按钮。房间运营卡此前同时带 1px 边框和投影，两种层级语言叠在同一张卡上。
- 内容区宽度上限由 1920px 收敛到 1240px。此前表单控件与提交按钮会被栅格拉到整块面板的宽度，在 1440px 屏上出现约 1650px 的下拉框和横贯整幅的按钮色块。表单控件改为按可读宽度收敛，提交按钮按内容取宽。
- 危险操作改为描边按钮，悬停时才填实。此前「取消任务」「撤回可入住状态」「替换入住凭证」与各页主操作是同样面积、同样饱和度的实心块，两者视觉等权。确认弹窗与既有二次确认逻辑不变。

### 房态时间轴状态可读性

- 近期房态的空闲、在住、入住、退房改用带颜色语义的标记，对比度均不低于 4.79:1。此前四种状态都是 11px 的次要灰字（`#64748b` 在 `#f8fafc` 上仅 4.55:1），空闲与在住只靠一层几乎不可见的浅蓝底区分，且在住格用青色边框配蓝色底，两个不相干色相叠在同一枚色块上。
- 时间轴改为等分列铺满卡片宽度，7 天不再折出只含一格的第二行；窄屏退化为带吸附的横向滚动。「今天」标记移入标记列，日期不再被挤到换行。

### 对比度与浏览器表面

- 次要文字 `--muted` 由 `#64748b` 提高到 `#57637a`，白底对比度由 4.76:1 升至 6.05:1。侧栏分组标签与版本号此前使用 `#94a3b8`，白底仅 2.56:1，不满足最低对比度要求，现统一使用次要文字色。
- 输入占位符此前沿用浏览器默认灰，现提高到 4.76:1，并与已填值保持可区分。
- 补齐文本选区、输入光标与滚动条配色。禁用态光标由 `wait` 改为 `not-allowed`，等待光标只保留给真正提交中的按钮。

### 认证页样式缓存修复

- 登录、修改初始密码和账号安全三页的样式表补上发布版本查询串。`/static` 以未配置 `Cache-Control` 的静态目录挂载，浏览器对这类响应走启发式缓存，版本查询串是唯一的缓存失效手段；此前只有后台外壳携带该查询串，认证页在发版后可能继续使用浏览器缓存中的旧样式。新增静态契约与集成测试各一项，分别锁定两套外壳的模板写法和登录页渲染出的已解析版本号。
- 认证页此前直接输出没有任何样式的 `<p role="alert">`，现复用后台既有提示组件。三页的表单标签改为包裹输入框，与后台字段写法一致，`for` 与 `id` 关联保持不变。

### 补齐缺失样式

- `.button-row`、`.inline-form`、`.verification-list` 和 `h4` 在模板中已被使用但没有任何样式规则，相关元素此前落回浏览器默认呈现。知识库候选卡内的小标题因此比同卡片的条目标题更抢眼，层级是反的。

### 边界

- 本版本只改变呈现，不新增数据库迁移、依赖、模型调用或运行配置，不改变退款、价格、房态、订单等业务规则，也不改变企业微信与百居易的对接行为。
- 页面地址、表单字段名称、提交目标、CSRF 与二次确认逻辑均未改变。

## [1.4.0] - 2026-09-03

### 后台表单令牌迁至服务端

- 任务、房源、客诉、客户和审批五个家族的一次性表单令牌改由服务端 nonce 持有，签名会话不再存放任何业务表单令牌。此前令牌按实体编号累积写入会话且只在提交成功时删除，浏览详情页而不提交即永久驻留；条目足够多时 `Set-Cookie` 会越过浏览器 4096 字节上限、整条 Cookie 被丢弃，表现为随机掉登录和「表单令牌无效或已使用」。实测连续浏览 60 个任务详情页后，会话由约 4000 字节降为恒定 227 字节。
- 令牌用途按实体隔离，跨实体重放继续被拒绝；审批确认令牌与审批单的绑定关系首次由测试锁定。签发时顺带清除迁移前遗留的会话令牌字典，既有臃肿会话在下次打开详情页时立即收缩。
- 运营四家族令牌有效期为 8 小时，与管理员会话闲置上限一致，保持迁移前的行为。审批确认沿用 15 分钟：超时必须刷新页面、重新读取当前房态与金额后再确认，而不是提交一个久放的旧表单。
- 页面地址、表单字段、导航名称和模板均未改变。

### 后台可用性与安全修复

- 修复知识页连续打开第九次返回 429 的问题。会话集合上限与服务端作用域上限同为 8，而裁剪发生在签发之后，导致裁剪分支从未执行、第九次打开即被拒绝。
- 修复管理员表单令牌占满全局配额后未登录访客无法取得登录令牌、登录页返回 429 的问题。现为未认证登录单独预留额度，预留量不超过总量的五分之一。

### 运行诊断

- 常驻后台任务意外结束时记录带调用栈的错误日志。此前这类任务由强引用持有，异常既不会被 asyncio 报告也无人记录，运维只能看到健康检查转为降级却拿不到原因。
- 日志脱敏过滤器覆盖异常栈与调用栈文本，密钥、令牌和回调签名在栈中同样被替换，多行结构与帧信息保留。此前 traceback 会绕过全部脱敏规则，项目因此无法输出任何带栈日志。九个周期任务与引导期日志据此补上调用栈。

### 边界

- 本版本不新增数据库迁移、依赖、模型调用或运行配置，不改变退款、价格、房态、订单等业务规则，也不改变企业微信与百居易的对接行为。

## [1.3.14] - 2026-09-02

### 运营台风险优先信息架构

- 运营总览拆分为「需要人工处理的风险」和「今日周转与工作量」两组语义分区，新增逾期任务指标，口径为服务日期早于本地当日且任务未进入终态，与同组「待办任务」同源；房态页的逾期数只统计启用房源，存在停用房源遗留任务时两处可以不同。指标为零时不再使用风险配色，启用房间数移入当前房态面板。
- 房态与运营页按同步可信度、今日周转、逾期任务和运营准备对房间排序，并以房源标题和主键做确定性兜底；今天需要处理的房间保留完整卡片，稳定房间折叠为紧凑列表，原有紧凑总览表保持不变。
- 任务中心快速队列收敛为四项开放队列，完整筛选改为默认收起、有生效条件时自动展开的原生折叠；包括已失效在内的全部筛选能力保持不变，移动端不必滚过整面筛选墙才能看到任务结果。

### 房态信息与文案

- 修正近期房态时间轴范围文案：页面此前只声明「未来 N 天」，实际渲染包含已过去的 2 天与今天，现按真实起止日期展示。
- 统一房态事实、运营准备、同步可信度和下一步的视觉语法：同步不可信单独声明，不再混入入住状态取色；琥珀色只表示今日周转，运营准备除维修和可入住外使用中性色；存在逾期任务时下一步转为风险配色。
- 房间标题改为真实标题层级，近期房态改为有序列表；任务中心移动端卡片补充逾期标记，状态取色与桌面表格一致。总览指标在窄屏改为单列紧凑行。

### 边界

- 本版本不修改业务规则、数据库结构、依赖、模型调用或运行配置，不新增前端框架、组件库或图表库，也不改变导航名称、URL、表单字段和任务状态机。

## [1.3.13] - 2026-09-01

### 住宿问题语义泛化

- 固定民宿提示词从设施故障扩展到影响当前入住的设施或住宿环境问题；噪音、异味等开放表达由同一结构化归属进入待确认人工处理，不增加关键词表或第二次模型调用。
- 结构化归属为民宿或不确定时保留经过安全清洗的场景化建议，不再仅因整体置信度较低而替换为设备停用文案。
- 维修任务、员工通知和客人回复继续按固定顺序执行；任务创建成功后才向客人声明已提交人工处理。

### 安全与边界

- 拆卸、接触线路、重置房间设备、追问和虚假承诺仍由逐句安全策略过滤；过滤后没有安全建议时继续使用统一降级文案。
- 明确属于客人私人物品或无关外部场所的问题仍不创建民宿任务；紧急事件及退款、价格、房态和订单等敏感边界保持不变。
- 本版本不增加数据库迁移、任务类型、依赖、模型调用或运行配置。

## [1.3.12] - 2026-09-01

### 设施故障语义泛化

- 结构化设施归属改为开放式故障主路由，不再因本地关键词未命中而丢弃模型已经识别的民宿设施异常；“洗衣机不出水”、制冷不足、水流过小、有声无画等不同说法均进入同一待确认维修流程。
- 设施归属不确定或模型低置信时创建待确认任务并使用通用安全建议；模型字段缺失或不可用时继续由本地明确故障信号兜底。
- 设施查看和维修请求不再先发送中立安抚，统一等待“简单建议 + 已提交管家人工处理”的最终回复。

### 安全与边界

- 明确的私人物品和外部设施仍不创建民宿维修任务；紧急事件、危险操作过滤、任务成功后才能声明提交人工等确定性安全门保持不变。
- 本版本不增加设备词典、安全档位、模型调用、依赖或数据库迁移，也不放宽退款、赔偿、价格和预订等敏感操作。

## [1.3.11] - 2026-09-01

### 修复

- 修正“确认房间开关是否已开启”等设备状态建议被误判为向客人追问的问题；只有索取客人补充说明的表达才会被设施回复安全门删除。
- 同步真实事务集成测试到模型建议输入，继续验证维修任务、员工通知和客人回复在同一事务中依次提交。

## [1.3.10] - 2026-09-01

### 设施故障回复

- 普通民宿设施故障改为由现有主模型请求直接生成一至两条针对性低风险建议，不再维护照明、供水、门锁等固定安全档位。
- 新设施首次出现时可沿用同一民宿提示词理解故障；模型不再额外调用一次分类请求，也不再要求客人补充描述后才提交人工。
- 维修任务创建成功后才向客人统一声明已提交管家人工处理；模型不可用、字段缺失、归属未知或低置信时使用通用停用建议。

### 安全与边界

- 发送前继续删除拆卸、接触线路、重启或重置房间网络设备、反复点火、强腐蚀药剂、追问、虚假人员及修复承诺等不安全内容。
- 明确属于客人私人物品或外部场所的故障仍不创建民宿维修任务；火灾、燃气、触电等紧急事件仍在模型调用前进入确定性安全流程。
- 本版本不增加数据库迁移、依赖或模型调用次数，不修改退款、赔偿、价格、实时房态来源和消息发送状态机。

## [1.3.9] - 2026-09-01

### 设施故障处理

- 民宿设施故障不再依赖固定设备名单；灯、马桶、窗帘、烘干机等首次出现的故障也会进入统一维修流程。
- 普通故障直接回复本地固定的简单安全建议，并明确告知已提交人工处理，不再只发送中立安抚或要求客人继续描述症状。
- 明确属于客人私人物品或景区、商场等外部设施的问题不会创建民宿维修任务；未说明归属的房间渠道短句默认按民宿设施处理。
- 冒烟、火花、焦味和漏电等危险信号仍优先进入紧急安全流程；模型不能生成拆机、带电或其他危险排查步骤。

### 可靠性与边界

- 维修任务、员工通知和客人回复保持同一事务及固定顺序；任务创建失败时不会向客人声称已提交人工。
- 模型分类缺失、无效、低置信或不可用时使用通用安全降级建议；不新增数据库迁移、依赖或持久化设备别名表。
- 本版本不修改退款、赔偿、价格、实时房态来源或人员到场及完成时间承诺。

## [1.3.8] - 2026-08-31

### 供应链安全

- 新增由 Python 3.12 生成的生产依赖哈希锁，固定直接依赖、传递依赖、构建依赖及制品 SHA-256；镜像安装项目时禁止重新解析依赖。
- 将 `cryptography` 升级并锁定为 `50.0.1`，排除已命中安全公告的旧版本系列；发布前软件组成扫描未发现已知漏洞。
- Python 3.12 slim 基础镜像固定到完整 digest，删除不安全的可信主机配置，只允许通过 TLS 索引按哈希安装依赖。
- 生产 API 改用固定 UID/GID `10001:10001` 非 root 运行；构建上下文排除密钥、版本库、虚拟环境、运行数据、备份、测试和受保护总结。

### 后台安全

- `/employee` 后台响应增加 `frame-ancestors 'none'`、`X-Frame-Options: DENY` 和 `Referrer-Policy: no-referrer`；公开健康检查和静态资源不附加后台专用策略。

### 持续集成

- 新增最小 GitHub Actions 门禁，使用只读仓库权限和固定提交 SHA，覆盖 Ruff、mypy、无真实外联测试、全新数据库迁移和生产镜像构建。

### 部署要求与边界

- 非 root 镜像启动前，私有上传宿主机目录必须由 UID/GID `10001:10001` 可读写；不满足时应用写入探针会拒绝启动。
- 本版本不增加数据库迁移或真实模型调用，不修改退款、价格决策、实时房态来源、订单和消息发送状态机。

## [1.3.7] - 2026-08-31

### 安全

- 预订审批改为只写、只读用途隔离密文，删除旧明文回退和历史回填入口；未清理记录缺少姓名或手机号密文时立即拒绝继续下单。
- 已按策略清理的审批在后台统一显示“已清理”，并禁止重新用于创建订单，避免将已删除的个人信息误判为空值继续流转。

### 数据保留

- 接入既有每日清理循环：已预订审批在退房满 30 天后清理密文，已拒绝或冲突审批在终态满 90 天后清理；开放中的审批不自动清理。
- 清理只移除姓名、手机号和特殊需求密文，保留审批状态、金额、时间和审计字段。

### 数据库与回滚

- 新增迁移 `0023_approval_pii_final`；迁移先校验全部未清理审批都具有必需密文，再删除姓名、手机号和特殊需求明文列。
- 本迁移不可普通降级；如需回滚，必须恢复升级前 PostgreSQL 备份并切回 v1.3.6 镜像。

### 边界

- 本版本不修改退款、价格决策、实时房态来源、模型调用预算或消息发送状态机。

## [1.3.6] - 2026-08-31

### 安全

- 为预订审批中的客人姓名、手机号和特殊需求增加用途隔离密文；新审批在兼容期内双写，业务读取优先使用密文。
- 员工审批页面改用不含 ORM 密文字段的只读视图，避免模板层接触持久化密文。
- 已存在密文但无法解密时拒绝静默回退旧明文，防止损坏或错误用途的密文被掩盖。

### 数据库

- 新增迁移 `0022_approval_pii`，只增加四个可空列并保留旧明文列，支持旧版本安全回滚。
- 提供按主键续跑、单批最多 100 条的幂等回填能力；发布前生产审批记录为零，因此本次不执行历史回填。

### 边界

- 本版本不删除旧审批明文列，不启用审批 PII 自动清理或保留期；这些变更留待回填验收后的独立阶段 2B。
- 本版本不增加模型调用，不修改退款、价格决策、实时房态来源或消息发送状态机。

## [1.3.5] - 2026-08-30

### 改进

- 为主回复、必要精炼和安全投递改写增加统一字符、调用轮次和输出 token 硬预算；超限时停止继续调用，并优先返回已经取得的确定性房态回执。
- 审核知识改为按当前问题进行受限的中英文相关性检索；FAQ 草稿只读取与候选标准问题相关的审核知识，不再注入固定前 100 条内容。
- 历史消息、当前问题、客户上下文、FAQ 候选和百居易工具结果分别执行有界裁剪；工具结果始终保持合法 JSON。

### 安全与边界

- 房态与参考价工具在任何百居易请求前统一校验严格 ISO 日期、武汉当天、365 天提前量及最多 30 晚住宿范围；非法参数不会触达百居易。
- 管理员 AI 调试复用同一住宿日期规则，避免调试入口与生产工具产生边界漂移。
- 本版本不增加向量数据库或外部检索服务，不修改预订、退款、价格决策、数据库结构或消息发送状态机。

## [1.3.4] - 2026-08-30

### 修复

- 将 API 私有上传目录挂载到可配置的宿主机持久目录，避免容器重建后数据库附件引用仍在但任务照片或房间凭证图片丢失。
- 应用启动时对私有上传目录执行一次真实写入、同步和删除探针；目录不可写时拒绝启动，避免系统在附件无法可靠保存的状态下继续运行。

### 部署安全

- 容器内私有上传路径固定为 `/app/data/private_uploads`，宿主机可通过 `PRIVATE_UPLOAD_HOST_DIR` 选择持久目录；生产切换前必须先迁移并核对旧容器文件。

## [1.3.3] - 2026-08-30

### 修复

- 修复任务中心、客户管理和知识库原生筛选表单因提交空字段而返回 422 的问题；空字段现在按未启用筛选处理，非法非空值仍保持强校验。
- 修复房间近期运营 3、7、14 天范围链接无法打开的问题，查询字符串通过白名单后再转换为服务所需整数。
- 筛选表单只把有效条件写入 URL，刷新、分享和翻页时继续使用服务端解析后的筛选状态；无脚本和旧链接场景由服务端容错。

## [1.3.2] - 2026-08-30

### 改进

- 为后台页面、状态提示、提交按钮、折叠面板、按钮和页签增加克制的交互反馈，并使用应用版本刷新 CSS 与 JavaScript 缓存。
- 移动端导航抽屉与遮罩支持可中断的开关动画，快速关闭后立即重开不会被旧回调反向关闭。

### 无障碍

- 抽屉关闭动画结束前继续保持焦点和可访问状态，结束后再恢复工作区与触发按钮。
- 遵循 `prefers-reduced-motion`，减少动态模式下关闭所有非必要动画并保留静态终态。

## [1.3.1] - 2026-08-30

### 修复

- 将任务生命周期迁移的 revision 缩短为 `0021_task_lifecycle`，适配 PostgreSQL 中 Alembic 默认的 32 字符版本字段；表结构和任务治理规则不变。
- 新增迁移编号长度回归门禁，避免 SQLite 未强制 `varchar` 长度而漏过生产兼容问题。

## [1.3.0] - 2026-08-30

### 新增

- 新增业务任务 `EXPIRED` 终态及来源、到期时间、关闭原因和关闭来源审计字段，明确区分“失去业务价值”“人工取消”和“已经完成”。
- 新增确定性的任务生命周期服务：订单取消或改期后立即复核关联任务，并由每小时、每批最多 100 条的本地巡检兜底；整个链路不调用模型。
- 任务中心新增逾期、待确认、待分派和已失效队列，管理员可查看失效历史，默认开放任务统计不会继续计入失效事项。

### 改进

- 同一次入住提醒失败及其派生人工联系任务只计为一个待关注事项，避免重复工作量和重复卡片。
- “七日房态”升级为按房间组织的近期运营看板，默认展示过去 2 天至未来 3 天，并支持未来 7 天、14 天切换。
- 入住状态严格来自百居易订单同步事实，运营准备度严格来自本地任务证据；同步过旧时明确显示“房态待确认”。
- 每个房间集中展示优先行动、开放与逾期任务、下次入住退房和可换行的近期时间轴，移动端无需横向滚动。

### 数据库

- 新增迁移 `0021_task_lifecycle_room_operations`，扩展任务生命周期字段、关闭员工外键、执行字段约束和状态到期索引，并按既有幂等键回填可证明的任务来源。

### 安全与边界

- 只有未分派、未开始、无检查清单且无附件的任务可以自动失效；已经出现执行证据的任务继续保留给人工判断。
- 实时房态继续只信百居易，本版本不新增 DeepSeek 调用，不改变退款、价格、凭证或消息发送安全门。

## [1.2.2] - 2026-08-29

### 修复

- 将大量待确认业务任务按房源汇总为工作队列卡片，保留真实待办总数；单条任务仍保留直达详情入口。
- 避免历史待确认任务在“待我关注”页面逐条展开，管理员可从汇总卡片进入对应任务列表集中处理。

## [1.2.1] - 2026-08-29

### 修复

- 将同一房源的重复入住提醒和凭证跟进归并为汇总卡片，保留真实待处理总数，避免历史事项逐条展开造成页面过载。
- 汇总卡片直接链接到对应房源的任务筛选结果，并显示最近更新时间。

## [1.2.0] - 2026-08-29

### 新增

- 新增“待我关注”和“房态与运营”页面，集中呈现人工事项、今日入住退房、开放任务和七日房态。
- 新增待处理客诉列表，以及任务、客户和知识库的可恢复 URL 筛选。

### 改进

- 房源列表升级为运营健康视图，集中显示房态、今日安排、开放任务、凭证版本和资料完整度。
- 客户列表升级为住宿状态与治理待办视图；客户详情按概览、住宿、服务、记忆和档案治理拆分页签。
- 任务中心展示可读房源、执行员工和逾期状态；审批页按真实状态限制可执行动作。
- 高风险表单统一确认语义、提交中状态和移动端触控布局；知识启停后返回可见列表反馈。

### 安全与边界

- 新页面仅聚合本地数据库中的安全投影，不读取客户消息正文，不回显门锁密码或入住指南。
- 本版本没有数据库迁移，不修改订单、任务、房态、凭证投递或消息发送状态机，也不新增外部接口调用。

## [1.1.0] - 2026-08-29

### 新增

- 为结构化客户记忆增加来源摘录、核验时间、状态原因和可清理的事件时间线。
- 在管理后台增加记忆来源、当前有效值对比和并发版本冲突提示。
- 增加客户记忆的证据分级、受控主题自动晋级和单主题唯一有效值约束。

### 改进

- 将动态订单、任务和客户上下文移出系统提示词，改为带硬预算的结构化用户上下文。
- 只有可回查的客户或员工原文才能形成强证据；模型推断不能覆盖强证据。
- 明确纠正可替换旧事实，其他同主题冲突会停止召回并进入人工复核。
- 为摘要维护和记忆审核增加乐观并发控制，避免过期结果静默覆盖新状态。
- 候选、争议、失效记忆和历史事件按治理周期自动退出或清理。

### 数据库

- 新增迁移 `0020_memory_trust_timeline`，升级客户记忆证据字段、事件表和唯一索引。

### 安全

- 增加提示词注入隔离和任务、预订副作用的当前意图授权门。
- 敏感、动态或指令型文本不会进入可召回的长期客户记忆。
