# 更新日志

本文件记录 YuMi 民宿 AI 的正式发布内容。版本号遵循语义化版本规则。

## [1.3.12] - 2026-09-01

### 设施故障语义泛化

- 结构化设施归属改为开放式故障主路由，不再因本地关键词未命中而丢弃模型已经识别的民宿设施异常；“洗衣机不出水”、制冷不足、水流过小、有声无画等不同说法均进入同一待确认维修流程。
- 设施归属不确定或模型低置信时创建待确认任务并使用通用安全建议；模型字段缺失或不可用时继续由本地明确故障信号兜底。
- 设施查看和维修请求不再先发送中立安抚，统一等待“简单建议 + 已提交管家人工处理”的最终回复。

### 安全与边界

- 明确的私人物品和外部设施仍不创建民宿维修任务；紧急事件、危险操作过滤、任务成功后才能声明提交人工等确定性安全门保持不变。
- 本版本不增加设备词典、安全档位、模型调用、依赖或数据库迁移，也不放宽退款、赔偿、价格和预订等敏感操作。

## [1.3.11] - 2026-09-01

### 修复

- 修正“确认房间开关是否已开启”等设备状态建议被误判为向客人追问的问题；只有索取客人补充说明的表达才会被设施回复安全门删除。
- 同步真实事务集成测试到模型建议输入，继续验证维修任务、员工通知和客人回复在同一事务中依次提交。

## [1.3.10] - 2026-09-01

### 设施故障回复

- 普通民宿设施故障改为由现有主模型请求直接生成一至两条针对性低风险建议，不再维护照明、供水、门锁等固定安全档位。
- 新设施首次出现时可沿用同一民宿提示词理解故障；模型不再额外调用一次分类请求，也不再要求客人补充描述后才提交人工。
- 维修任务创建成功后才向客人统一声明已提交管家人工处理；模型不可用、字段缺失、归属未知或低置信时使用通用停用建议。

### 安全与边界

- 发送前继续删除拆卸、接触线路、重启或重置房间网络设备、反复点火、强腐蚀药剂、追问、虚假人员及修复承诺等不安全内容。
- 明确属于客人私人物品或外部场所的故障仍不创建民宿维修任务；火灾、燃气、触电等紧急事件仍在模型调用前进入确定性安全流程。
- 本版本不增加数据库迁移、依赖或模型调用次数，不修改退款、赔偿、价格、实时房态来源和消息发送状态机。

## [1.3.9] - 2026-09-01

### 设施故障处理

- 民宿设施故障不再依赖固定设备名单；灯、马桶、窗帘、烘干机等首次出现的故障也会进入统一维修流程。
- 普通故障直接回复本地固定的简单安全建议，并明确告知已提交人工处理，不再只发送中立安抚或要求客人继续描述症状。
- 明确属于客人私人物品或景区、商场等外部设施的问题不会创建民宿维修任务；未说明归属的房间渠道短句默认按民宿设施处理。
- 冒烟、火花、焦味和漏电等危险信号仍优先进入紧急安全流程；模型不能生成拆机、带电或其他危险排查步骤。

### 可靠性与边界

- 维修任务、员工通知和客人回复保持同一事务及固定顺序；任务创建失败时不会向客人声称已提交人工。
- 模型分类缺失、无效、低置信或不可用时使用通用安全降级建议；不新增数据库迁移、依赖或持久化设备别名表。
- 本版本不修改退款、赔偿、价格、实时房态来源或人员到场及完成时间承诺。

## [1.3.8] - 2026-08-31

### 供应链安全

- 新增由 Python 3.12 生成的生产依赖哈希锁，固定直接依赖、传递依赖、构建依赖及制品 SHA-256；镜像安装项目时禁止重新解析依赖。
- 将 `cryptography` 升级并锁定为 `50.0.1`，排除已命中安全公告的旧版本系列；发布前软件组成扫描未发现已知漏洞。
- Python 3.12 slim 基础镜像固定到完整 digest，删除不安全的可信主机配置，只允许通过 TLS 索引按哈希安装依赖。
- 生产 API 改用固定 UID/GID `10001:10001` 非 root 运行；构建上下文排除密钥、版本库、虚拟环境、运行数据、备份、测试和受保护总结。

### 后台安全

- `/employee` 后台响应增加 `frame-ancestors 'none'`、`X-Frame-Options: DENY` 和 `Referrer-Policy: no-referrer`；公开健康检查和静态资源不附加后台专用策略。

### 持续集成

- 新增最小 GitHub Actions 门禁，使用只读仓库权限和固定提交 SHA，覆盖 Ruff、mypy、无真实外联测试、全新数据库迁移和生产镜像构建。

### 部署要求与边界

- 非 root 镜像启动前，私有上传宿主机目录必须由 UID/GID `10001:10001` 可读写；不满足时应用写入探针会拒绝启动。
- 本版本不增加数据库迁移或真实模型调用，不修改退款、价格决策、实时房态来源、订单和消息发送状态机。

## [1.3.7] - 2026-08-31

### 安全

- 预订审批改为只写、只读用途隔离密文，删除旧明文回退和历史回填入口；未清理记录缺少姓名或手机号密文时立即拒绝继续下单。
- 已按策略清理的审批在后台统一显示“已清理”，并禁止重新用于创建订单，避免将已删除的个人信息误判为空值继续流转。

### 数据保留

- 接入既有每日清理循环：已预订审批在退房满 30 天后清理密文，已拒绝或冲突审批在终态满 90 天后清理；开放中的审批不自动清理。
- 清理只移除姓名、手机号和特殊需求密文，保留审批状态、金额、时间和审计字段。

### 数据库与回滚

- 新增迁移 `0023_approval_pii_final`；迁移先校验全部未清理审批都具有必需密文，再删除姓名、手机号和特殊需求明文列。
- 本迁移不可普通降级；如需回滚，必须恢复升级前 PostgreSQL 备份并切回 v1.3.6 镜像。

### 边界

- 本版本不修改退款、价格决策、实时房态来源、模型调用预算或消息发送状态机。

## [1.3.6] - 2026-08-31

### 安全

- 为预订审批中的客人姓名、手机号和特殊需求增加用途隔离密文；新审批在兼容期内双写，业务读取优先使用密文。
- 员工审批页面改用不含 ORM 密文字段的只读视图，避免模板层接触持久化密文。
- 已存在密文但无法解密时拒绝静默回退旧明文，防止损坏或错误用途的密文被掩盖。

### 数据库

- 新增迁移 `0022_approval_pii`，只增加四个可空列并保留旧明文列，支持旧版本安全回滚。
- 提供按主键续跑、单批最多 100 条的幂等回填能力；发布前生产审批记录为零，因此本次不执行历史回填。

### 边界

- 本版本不删除旧审批明文列，不启用审批 PII 自动清理或保留期；这些变更留待回填验收后的独立阶段 2B。
- 本版本不增加模型调用，不修改退款、价格决策、实时房态来源或消息发送状态机。

## [1.3.5] - 2026-08-30

### 改进

- 为主回复、必要精炼和安全投递改写增加统一字符、调用轮次和输出 token 硬预算；超限时停止继续调用，并优先返回已经取得的确定性房态回执。
- 审核知识改为按当前问题进行受限的中英文相关性检索；FAQ 草稿只读取与候选标准问题相关的审核知识，不再注入固定前 100 条内容。
- 历史消息、当前问题、客户上下文、FAQ 候选和百居易工具结果分别执行有界裁剪；工具结果始终保持合法 JSON。

### 安全与边界

- 房态与参考价工具在任何百居易请求前统一校验严格 ISO 日期、武汉当天、365 天提前量及最多 30 晚住宿范围；非法参数不会触达百居易。
- 管理员 AI 调试复用同一住宿日期规则，避免调试入口与生产工具产生边界漂移。
- 本版本不增加向量数据库或外部检索服务，不修改预订、退款、价格决策、数据库结构或消息发送状态机。

## [1.3.4] - 2026-08-30

### 修复

- 将 API 私有上传目录挂载到可配置的宿主机持久目录，避免容器重建后数据库附件引用仍在但任务照片或房间凭证图片丢失。
- 应用启动时对私有上传目录执行一次真实写入、同步和删除探针；目录不可写时拒绝启动，避免系统在附件无法可靠保存的状态下继续运行。

### 部署安全

- 容器内私有上传路径固定为 `/app/data/private_uploads`，宿主机可通过 `PRIVATE_UPLOAD_HOST_DIR` 选择持久目录；生产切换前必须先迁移并核对旧容器文件。

## [1.3.3] - 2026-08-30

### 修复

- 修复任务中心、客户管理和知识库原生筛选表单因提交空字段而返回 422 的问题；空字段现在按未启用筛选处理，非法非空值仍保持强校验。
- 修复房间近期运营 3、7、14 天范围链接无法打开的问题，查询字符串通过白名单后再转换为服务所需整数。
- 筛选表单只把有效条件写入 URL，刷新、分享和翻页时继续使用服务端解析后的筛选状态；无脚本和旧链接场景由服务端容错。

## [1.3.2] - 2026-08-30

### 改进

- 为后台页面、状态提示、提交按钮、折叠面板、按钮和页签增加克制的交互反馈，并使用应用版本刷新 CSS 与 JavaScript 缓存。
- 移动端导航抽屉与遮罩支持可中断的开关动画，快速关闭后立即重开不会被旧回调反向关闭。

### 无障碍

- 抽屉关闭动画结束前继续保持焦点和可访问状态，结束后再恢复工作区与触发按钮。
- 遵循 `prefers-reduced-motion`，减少动态模式下关闭所有非必要动画并保留静态终态。

## [1.3.1] - 2026-08-30

### 修复

- 将任务生命周期迁移的 revision 缩短为 `0021_task_lifecycle`，适配 PostgreSQL 中 Alembic 默认的 32 字符版本字段；表结构和任务治理规则不变。
- 新增迁移编号长度回归门禁，避免 SQLite 未强制 `varchar` 长度而漏过生产兼容问题。

## [1.3.0] - 2026-08-30

### 新增

- 新增业务任务 `EXPIRED` 终态及来源、到期时间、关闭原因和关闭来源审计字段，明确区分“失去业务价值”“人工取消”和“已经完成”。
- 新增确定性的任务生命周期服务：订单取消或改期后立即复核关联任务，并由每小时、每批最多 100 条的本地巡检兜底；整个链路不调用模型。
- 任务中心新增逾期、待确认、待分派和已失效队列，管理员可查看失效历史，默认开放任务统计不会继续计入失效事项。

### 改进

- 同一次入住提醒失败及其派生人工联系任务只计为一个待关注事项，避免重复工作量和重复卡片。
- “七日房态”升级为按房间组织的近期运营看板，默认展示过去 2 天至未来 3 天，并支持未来 7 天、14 天切换。
- 入住状态严格来自百居易订单同步事实，运营准备度严格来自本地任务证据；同步过旧时明确显示“房态待确认”。
- 每个房间集中展示优先行动、开放与逾期任务、下次入住退房和可换行的近期时间轴，移动端无需横向滚动。

### 数据库

- 新增迁移 `0021_task_lifecycle_room_operations`，扩展任务生命周期字段、关闭员工外键、执行字段约束和状态到期索引，并按既有幂等键回填可证明的任务来源。

### 安全与边界

- 只有未分派、未开始、无检查清单且无附件的任务可以自动失效；已经出现执行证据的任务继续保留给人工判断。
- 实时房态继续只信百居易，本版本不新增 DeepSeek 调用，不改变退款、价格、凭证或消息发送安全门。

## [1.2.2] - 2026-08-29

### 修复

- 将大量待确认业务任务按房源汇总为工作队列卡片，保留真实待办总数；单条任务仍保留直达详情入口。
- 避免历史待确认任务在“待我关注”页面逐条展开，管理员可从汇总卡片进入对应任务列表集中处理。

## [1.2.1] - 2026-08-29

### 修复

- 将同一房源的重复入住提醒和凭证跟进归并为汇总卡片，保留真实待处理总数，避免历史事项逐条展开造成页面过载。
- 汇总卡片直接链接到对应房源的任务筛选结果，并显示最近更新时间。

## [1.2.0] - 2026-08-29

### 新增

- 新增“待我关注”和“房态与运营”页面，集中呈现人工事项、今日入住退房、开放任务和七日房态。
- 新增待处理客诉列表，以及任务、客户和知识库的可恢复 URL 筛选。

### 改进

- 房源列表升级为运营健康视图，集中显示房态、今日安排、开放任务、凭证版本和资料完整度。
- 客户列表升级为住宿状态与治理待办视图；客户详情按概览、住宿、服务、记忆和档案治理拆分页签。
- 任务中心展示可读房源、执行员工和逾期状态；审批页按真实状态限制可执行动作。
- 高风险表单统一确认语义、提交中状态和移动端触控布局；知识启停后返回可见列表反馈。

### 安全与边界

- 新页面仅聚合本地数据库中的安全投影，不读取客户消息正文，不回显门锁密码或入住指南。
- 本版本没有数据库迁移，不修改订单、任务、房态、凭证投递或消息发送状态机，也不新增外部接口调用。

## [1.1.0] - 2026-08-29

### 新增

- 为结构化客户记忆增加来源摘录、核验时间、状态原因和可清理的事件时间线。
- 在管理后台增加记忆来源、当前有效值对比和并发版本冲突提示。
- 增加客户记忆的证据分级、受控主题自动晋级和单主题唯一有效值约束。

### 改进

- 将动态订单、任务和客户上下文移出系统提示词，改为带硬预算的结构化用户上下文。
- 只有可回查的客户或员工原文才能形成强证据；模型推断不能覆盖强证据。
- 明确纠正可替换旧事实，其他同主题冲突会停止召回并进入人工复核。
- 为摘要维护和记忆审核增加乐观并发控制，避免过期结果静默覆盖新状态。
- 候选、争议、失效记忆和历史事件按治理周期自动退出或清理。

### 数据库

- 新增迁移 `0020_memory_trust_timeline`，升级客户记忆证据字段、事件表和唯一索引。

### 安全

- 增加提示词注入隔离和任务、预订副作用的当前意图授权门。
- 敏感、动态或指令型文本不会进入可召回的长期客户记忆。
